/**
 * Generates safe SQL alternatives for common dangerous patterns.
 * These are copy-paste-ready replacements shown in CLI output and PR comments.
 */
export declare function generateSafeAlternative(stmt: Record<string, unknown>, ruleId: string, originalSql: string): string | null;
//# sourceMappingURL=alternatives.d.ts.map