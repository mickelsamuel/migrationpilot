/**
 * GitHub Action entry point for MigrationPilot.
 * Analyzes migration files changed in a PR and posts safety reports as PR comments.
 *
 * Bundled with esbuild into dist/action/index.js. The pipeline itself lives in
 * run.ts so it can be exercised by tests; this file exists to start it.
 */
export {};
//# sourceMappingURL=index.d.ts.map