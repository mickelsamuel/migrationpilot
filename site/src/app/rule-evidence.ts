// Generated by scripts/generate-rule-evidence.ts — do not edit by hand.
//
// The CLI findings are the real output of `formatCliOutput` over each rule's
// flagged example, with the other rules' blocks removed. Related rules come from
// docs/rules/*.md. Regenerate after changing an example, a rule message, or a doc:
//
//   npx tsx scripts/generate-rule-evidence.ts

/** What the CLI prints for this rule, on this rule's own flagged example. */
export const cliFindingByRuleId: Record<string, string> = {
  MP001: "✗ [MP001] CRITICAL (line 1)\n  CREATE INDEX \"idx_users_email\" without CONCURRENTLY will lock all writes on \"users\" for the entire duration of index creation.\n\n  Safe alternative:\n  SET lock_timeout = '5s';\n  -- Clear an invalid index left by an earlier failed attempt. Do not use\n  -- IF NOT EXISTS here: it would skip the build and report success over it.\n  DROP INDEX CONCURRENTLY IF EXISTS idx_users_email;\n  CREATE INDEX CONCURRENTLY idx_users_email ON users (email);\n  -- If a UNIQUE or PRIMARY KEY constraint owns idx_users_email, the drop is refused.\n  -- Rebuild in place instead: REINDEX INDEX CONCURRENTLY idx_users_email;\n\n  Why: Without CONCURRENTLY, PostgreSQL holds a SHARE lock on the table for the entire index build. Reads keep working; every INSERT, UPDATE and DELETE blocks until the build finishes. On a table with millions of rows that is minutes of writes queued behind one statement, and every connection waiting on them.\n  Docs: https://migrationpilot.dev/rules/mp001",
  MP002: "✗ [MP002] CRITICAL (line 1)\n  SET NOT NULL on \"users\".\"email\" requires a full table scan under ACCESS EXCLUSIVE lock. Use the CHECK constraint pattern for zero-downtime.\n\n  Safe alternative:\n  -- Step 1: Add CHECK constraint (brief ACCESS EXCLUSIVE lock, no table scan)\n  ALTER TABLE users ADD CONSTRAINT users_email_not_null\n    CHECK (email IS NOT NULL) NOT VALID;\n  \n  -- Step 2: Validate constraint (SHARE UPDATE EXCLUSIVE, allows reads + writes)\n  ALTER TABLE users VALIDATE CONSTRAINT users_email_not_null;\n  \n  -- Step 3: Set NOT NULL using validated constraint (PG 12+, instant: uses existing CHECK)\n  ALTER TABLE users ALTER COLUMN email SET NOT NULL;\n  \n  -- Step 4: Clean up the CHECK constraint\n  ALTER TABLE users DROP CONSTRAINT users_email_not_null;\n\n  Why: SET NOT NULL requires a full table scan while holding an ACCESS EXCLUSIVE lock. The CHECK constraint + VALIDATE pattern splits this into a brief lock for adding the constraint and a longer scan under a weaker lock that allows reads and writes.\n  Docs: https://migrationpilot.dev/rules/mp002",
  MP003: "✗ [MP003] CRITICAL (line 1)\n  ADD COLUMN \"public_id\" DEFAULT gen_random_uuid() on \"orders\" uses a volatile default, so PostgreSQL rewrites the entire table and every index on it while holding ACCESS EXCLUSIVE. gen_random_uuid() has to be evaluated once per existing row, which rules out the pg_attribute.attmissingval fast path a constant default takes.\n\n  Safe alternative:\n  -- Add the column with no default, so the catalog write is all it costs:\n  ALTER TABLE orders ADD COLUMN public_id <type>;\n  \n  -- Fill it in batches, outside any long transaction. Repeat until 0 rows:\n  UPDATE orders SET public_id = gen_random_uuid()\n  WHERE public_id IS NULL\n    AND id IN (SELECT id FROM orders WHERE public_id IS NULL LIMIT 10000);\n  \n  -- Only then attach the default, for rows written from here on:\n  ALTER TABLE orders ALTER COLUMN public_id SET DEFAULT gen_random_uuid();\n\n  Why: A non-volatile default is evaluated once and stored in pg_attribute.attmissingval, so ADD COLUMN touches no heap pages. A volatile default cannot be stored that way. PostgreSQL has to evaluate it separately for every existing row, which means writing a fresh copy of the table and all of its indexes while holding ACCESS EXCLUSIVE. Reads and writes are blocked for the whole rewrite, and the operation needs enough free disk for a second copy of the table before it can finish.\n  Docs: https://migrationpilot.dev/rules/mp003",
  MP004: "✗ [MP004] CRITICAL (line 1)\n  DDL statement acquires ACCESS EXCLUSIVE lock without a preceding SET lock_timeout. Without a timeout, this statement could block the lock queue indefinitely if it can't acquire the lock, causing cascading query failures.\n\n  Safe alternative:\n  -- Set a timeout so DDL fails fast instead of blocking the queue\n  SET lock_timeout = '5s';\n  ALTER TABLE users ADD COLUMN bio TEXT\n  RESET lock_timeout;\n\n  Why: Without lock_timeout, if the table is locked by another query, your DDL waits indefinitely. All subsequent queries pile up behind it in the lock queue, causing cascading timeouts across your application. GoCardless enforces a 750ms lock_timeout for this reason.\n  Docs: https://migrationpilot.dev/rules/mp004",
  MP005: "✗ [MP005] CRITICAL (line 1)\n  FK constraint \"fk_orders_user\" on \"orders\" → \"users\" without NOT VALID. This scans the entire table under ACCESS EXCLUSIVE lock, blocking all reads and writes.\n\n  Safe alternative:\n  -- Step 1: Add FK with NOT VALID (brief lock, no scan)\n  ALTER TABLE orders ADD CONSTRAINT fk_orders_user\n    FOREIGN KEY (...) REFERENCES users (...) NOT VALID;\n  \n  -- Step 2: Validate separately (SHARE UPDATE EXCLUSIVE, allows reads + writes)\n  ALTER TABLE orders VALIDATE CONSTRAINT fk_orders_user;\n\n  Why: Adding a foreign key validates all existing rows while holding an ACCESS EXCLUSIVE lock. NOT VALID skips validation during creation, then VALIDATE CONSTRAINT checks rows with a SHARE UPDATE EXCLUSIVE lock that allows reads and writes.\n  Docs: https://migrationpilot.dev/rules/mp005",
  MP006: "✗ [MP006] CRITICAL (line 1)\n  VACUUM FULL on \"users\" rewrites the entire table under ACCESS EXCLUSIVE lock. This blocks ALL reads and writes for the entire duration.\n\n  Safe alternative:\n  -- Use pg_repack instead (no ACCESS EXCLUSIVE lock during rewrite):\n  -- Install: CREATE EXTENSION pg_repack;\n  -- Run: pg_repack --table users --no-superuser-check\n\n  Why: VACUUM FULL rewrites the entire table to a new file, holding an ACCESS EXCLUSIVE lock for the full duration. On large tables this can take hours. Use regular VACUUM or pg_repack for online table compaction instead.\n  Docs: https://migrationpilot.dev/rules/mp006",
  MP007: "✗ [MP007] CRITICAL (line 1)\n  ALTER COLUMN TYPE on \"users\".\"age\" rewrites the entire table under ACCESS EXCLUSIVE lock, blocking all reads and writes.\n\n  Safe alternative:\n  -- Use the expand-contract pattern:\n  -- Step 1: Add new column with desired type\n  ALTER TABLE users ADD COLUMN age_new <new_type>;\n  \n  -- Step 2: Backfill in batches\n  UPDATE users SET age_new = age::<new_type>\n    WHERE id IN (SELECT id FROM users WHERE age_new IS NULL LIMIT 10000);\n  \n  -- Step 3: Create trigger to sync writes (during backfill)\n  -- Step 4: Swap columns (brief lock)\n  -- Step 5: Drop old column\n\n  Why: Changing a column type rewrites every row in the table while holding an ACCESS EXCLUSIVE lock that blocks all reads and writes. On large tables this can take hours, causing extended downtime.\n  Docs: https://migrationpilot.dev/rules/mp007",
  MP008: "✗ [MP008] CRITICAL (line 3)\n  Multiple DDL statements in a single transaction. Locks are held for the ENTIRE transaction, the combined duration of all DDL operations. Run each DDL in its own transaction.\n\n  Why: When multiple DDL statements run in one transaction, all locks are held until COMMIT. This multiplies the downtime window: the total lock time is the sum of all DDL operations, not just the longest one.\n  Docs: https://migrationpilot.dev/rules/mp008",
  MP009: "⚠ [MP009] WARNING (line 1)\n  DROP INDEX \"idx_users_email\" without CONCURRENTLY acquires ACCESS EXCLUSIVE lock, blocking all reads and writes on the table.\n\n  Safe alternative:\n  DROP INDEX CONCURRENTLY idx_users_email\n\n  Why: DROP INDEX without CONCURRENTLY takes an ACCESS EXCLUSIVE lock on the table, blocking all reads and writes until the index is fully dropped. DROP INDEX CONCURRENTLY avoids this by using a multi-phase approach.\n  Docs: https://migrationpilot.dev/rules/mp009",
  MP010: "⚠ [MP010] WARNING (line 1)\n  Renaming column \"name\" to \"full_name\" on \"users\" will break any application queries or views referencing the old column name.\n\n  Safe alternative:\n  -- Step 1: Add new column\n  ALTER TABLE users ADD COLUMN full_name <same_type>;\n  \n  -- Step 2: Backfill data (in batches for large tables)\n  UPDATE users SET full_name = name WHERE full_name IS NULL;\n  \n  -- Step 3: Update application code to use \"full_name\"\n  -- Step 4: Stop writing to \"name\"\n  -- Step 5: Drop old column (after verifying no reads)\n  ALTER TABLE users DROP COLUMN name;\n\n  Why: Renaming a column takes an ACCESS EXCLUSIVE lock and instantly breaks every application query, view, and function referencing the old name. Use the expand-contract pattern: add a new column, migrate data, update code, then drop the old column.\n  Docs: https://migrationpilot.dev/rules/mp010",
  MP011: "⚠ [MP011] WARNING (line 1)\n  UPDATE on \"users\" without a WHERE clause rewrites every row in a single transaction. For large tables, this generates massive WAL, can bloat the table, and holds ROW EXCLUSIVE lock for the entire duration.\n\n  Safe alternative:\n  -- Backfill in batches to reduce lock duration and WAL volume:\n  DO $$\n  DECLARE\n    batch_size INT := 10000;\n    rows_updated INT;\n  BEGIN\n    LOOP\n      UPDATE users\n      SET <column> = <value>\n      WHERE <column> IS NULL  -- or your condition\n      AND ctid IN (\n        SELECT ctid FROM users\n        WHERE <column> IS NULL\n        LIMIT batch_size\n        FOR UPDATE SKIP LOCKED\n      );\n      GET DIAGNOSTICS rows_updated = ROW_COUNT;\n      EXIT WHEN rows_updated = 0;\n      COMMIT;\n      PERFORM pg_sleep(0.1);  -- Brief pause to let other queries through\n    END LOOP;\n  END $$;\n\n  Why: A full-table UPDATE generates massive WAL, bloats the table, and holds a ROW EXCLUSIVE lock for the entire duration. On tables with millions of rows, this can take hours and cause replication lag, disk pressure, and degraded performance.\n  Docs: https://migrationpilot.dev/rules/mp011",
  MP012: "⚠ [MP012] WARNING (line 2)\n  ALTER TYPE \"status\" ADD VALUE 'archived' in a transaction. On PG 12+ this works but takes ACCESS EXCLUSIVE on the enum type. Consider running outside the transaction to minimize lock duration.\n\n  Why: On PostgreSQL versions before 12, ALTER TYPE ADD VALUE inside a transaction raises a runtime error, failing your migration entirely. On PG 12+ it works but takes an ACCESS EXCLUSIVE lock on the type, which can block concurrent queries.\n  Docs: https://migrationpilot.dev/rules/mp012",
  MP013: "⚠ [MP013] WARNING (line 2)\n  DDL acquires ACCESS EXCLUSIVE lock on a table with 50,000 queries from api. Top query: \"SELECT * FROM users WHERE id = $1...\" (50,000 calls, 0.8ms avg).\n\n  Safe alternative:\n  -- Set a short lock_timeout to fail fast instead of blocking queries:\n  SET lock_timeout = '3s';\n  ALTER TABLE users ADD COLUMN last_seen_at timestamptz\n  RESET lock_timeout;\n  \n  -- Consider running during low-traffic hours.\n  -- If lock acquisition fails, retry with exponential backoff.\n\n  Why: Running DDL on tables with high query volume amplifies the blast radius. Even brief locks cause significant query queuing when thousands of queries per second hit the table, leading to cascading timeouts across dependent services.\n  Docs: https://migrationpilot.dev/rules/mp013",
  MP014: "⚠ [MP014] WARNING (line 2)\n  Long-held ACCESS EXCLUSIVE on a table with 50,000,000 rows (42.9 GB, 4 indexes). Lock duration will scale with table size, blocking ALL reads and writes.\n\n  Safe alternative:\n  -- For large tables, consider:\n  -- 1. Set a lock_timeout to fail fast:\n  SET lock_timeout = '5s';\n  ALTER TABLE users ALTER COLUMN bio TYPE text\n  RESET lock_timeout;\n  \n  -- 2. Run during maintenance windows\n  -- 3. If this is an index creation, ensure CONCURRENTLY is used\n  -- 4. For column additions with defaults, consider adding without default then backfilling\n\n  Why: DDL operations on large tables take proportionally longer. Lock duration scales with row count and table size. What takes seconds on a small table can take minutes or hours on a table with millions of rows.\n  Docs: https://migrationpilot.dev/rules/mp014",
  MP015: "⚠ [MP015] WARNING (line 1)\n  ADD COLUMN \"id\" with SERIAL on \"users\" creates an implicit sequence and may cause table rewrite.\n\n  Safe alternative:\n  -- PG 10+: Use GENERATED ALWAYS AS IDENTITY instead of SERIAL:\n  ALTER TABLE users ADD COLUMN id integer GENERATED ALWAYS AS IDENTITY;\n\n  Why: SERIAL/BIGSERIAL creates an implicit sequence and DEFAULT, which on PG versions before 11 causes a full table rewrite. Use GENERATED ALWAYS AS IDENTITY or manually create the sequence to avoid the rewrite.\n  Docs: https://migrationpilot.dev/rules/mp015",
  MP016: "⚠ [MP016] WARNING (line 1)\n  FK constraint \"fk_user\" on \"orders\"(user_id) → \"users\" has no matching index. Without an index, cascading updates/deletes cause sequential scans.\n\n  Safe alternative:\n  -- Create index on FK columns (CONCURRENTLY to avoid blocking)\n  CREATE INDEX CONCURRENTLY idx_orders_user_id ON orders (user_id);\n\n  Why: Without an index on foreign key columns, PostgreSQL performs sequential scans during cascading deletes and updates. On large tables, this causes long-held SHARE locks on the parent table and severely degraded write performance.\n  Docs: https://migrationpilot.dev/rules/mp016",
  MP017: "⚠ [MP017] WARNING (line 1)\n  DROP COLUMN \"old_field\" on \"users\" takes ACCESS EXCLUSIVE lock. Running application code referencing this column will break immediately.\n\n  Safe alternative:\n  -- Safe multi-deploy approach:\n  -- Deploy 1: Remove all application code references to \"old_field\"\n  -- Deploy 2: Drop the column with a short lock timeout\n  SET lock_timeout = '5s';\n  ALTER TABLE users DROP COLUMN old_field;\n  RESET lock_timeout;\n\n  Why: DROP COLUMN takes an ACCESS EXCLUSIVE lock and instantly breaks any application code still selecting or inserting that column. Always remove all code references in a prior deploy before dropping the column.\n  Docs: https://migrationpilot.dev/rules/mp017",
  MP018: "⚠ [MP018] WARNING (line 1)\n  SET NOT NULL on \"users\".\"email\" scans the entire table under ACCESS EXCLUSIVE lock to verify no NULLs.\n\n  Safe alternative:\n  -- PG 12+ safe approach:\n  -- Step 1: Add CHECK constraint NOT VALID (instant, no scan)\n  ALTER TABLE users ADD CONSTRAINT users_email_not_null\n    CHECK (email IS NOT NULL) NOT VALID;\n  \n  -- Step 2: Validate separately (SHARE UPDATE EXCLUSIVE, allows reads + writes)\n  ALTER TABLE users VALIDATE CONSTRAINT users_email_not_null;\n  \n  -- Step 3: SET NOT NULL is now instant (PG sees the validated CHECK)\n  ALTER TABLE users ALTER COLUMN email SET NOT NULL;\n\n  Why: SET NOT NULL scans every row to verify no NULLs while holding an ACCESS EXCLUSIVE lock. On PostgreSQL 12+, adding a CHECK (col IS NOT NULL) NOT VALID constraint first, validating it, then SET NOT NULL makes the final step instant.\n  Docs: https://migrationpilot.dev/rules/mp018",
  MP019: "⚠ [MP019] WARNING (line 2)\n  ACCESS EXCLUSIVE lock on \"users\" while 200 active connections exist. All 200 connections will queue, causing cascading timeouts.\n\n  Safe alternative:\n  -- Run during a low-traffic window and use a short lock_timeout:\n  SET lock_timeout = '3s';\n  ALTER TABLE users ALTER COLUMN email TYPE varchar(320)\n  RESET lock_timeout;\n  \n  -- If lock acquisition fails, retry with exponential backoff.\n  -- Consider: is there a non-locking alternative? (e.g., CONCURRENTLY for indexes)\n\n  Why: Taking an ACCESS EXCLUSIVE lock when many connections are active means all those connections will queue waiting for the lock. This causes cascading timeouts, connection pool exhaustion, and can take down dependent services.\n  Docs: https://migrationpilot.dev/rules/mp019",
  MP020: "⚠ [MP020] WARNING (line 1)\n  Long-running DDL without a preceding SET statement_timeout. This operation could hold locks for an extended time if it runs longer than expected.\n\n  Safe alternative:\n  -- Set a timeout so the operation is killed if it runs too long\n  SET statement_timeout = '30s';\n  ALTER TABLE orders VALIDATE CONSTRAINT fk_orders_user\n  RESET statement_timeout;\n\n  Why: Without statement_timeout, a DDL operation that encounters unexpected conditions (bloated table, heavy WAL, slow I/O) can hold locks for hours, turning a routine migration into a full outage.\n  Docs: https://migrationpilot.dev/rules/mp020",
  MP021: "⚠ [MP021] WARNING (line 1)\n  REINDEX INDEX \"idx_users_email\" without CONCURRENTLY blocks all writes (or reads for tables). On PostgreSQL 17, use REINDEX CONCURRENTLY instead.\n\n  Safe alternative:\n  REINDEX INDEX CONCURRENTLY idx_users_email\n\n  Why: REINDEX without CONCURRENTLY acquires ACCESS EXCLUSIVE (table) or SHARE (index) locks, blocking all queries for the duration. REINDEX CONCURRENTLY (PG 12+) builds the new index without blocking reads or writes.\n  Docs: https://migrationpilot.dev/rules/mp021",
  MP022: "⚠ [MP022] WARNING (line 1)\n  DROP TABLE \"users\" CASCADE will silently drop all dependent objects (views, foreign keys, policies, triggers). Drop dependents explicitly instead.\n\n  Safe alternative:\n  -- First check dependent objects:\n  -- SELECT deptype, classid::regclass, objid, objsubid\n  --   FROM pg_depend\n  --   WHERE refobjid = 'users'::regclass;\n  -- Then drop dependents explicitly before the target:\n  DROP TABLE users;\n\n  Why: CASCADE silently drops all dependent objects without listing them: views, foreign keys, policies, and triggers. You may unintentionally destroy critical production objects that other services depend on.\n  Docs: https://migrationpilot.dev/rules/mp022",
  MP023: "⚠ [MP023] WARNING (line 1)\n  CREATE TABLE \"users\" without IF NOT EXISTS will fail if the table already exists. Use IF NOT EXISTS for idempotent migrations.\n\n  Safe alternative:\n  CREATE TABLE IF NOT EXISTS users (id BIGINT PRIMARY KEY)\n\n  Why: Without IF NOT EXISTS, re-running a migration fails with \"relation already exists\". Idempotent migrations are safer for retry and rollback scenarios, and required by many deployment pipelines. Concurrent index builds are excluded on purpose: there, IF NOT EXISTS matches an index a failed build left INVALID and reports success without rebuilding it, so the retry-safe form is a preceding DROP INDEX CONCURRENTLY IF EXISTS instead (MP070).\n  Docs: https://migrationpilot.dev/rules/mp023",
  MP024: "⚠ [MP024] WARNING (line 1)\n  DROP TYPE \"status\" will destroy the type and fail if any columns use it. PostgreSQL cannot remove individual enum values. The type must be recreated.\n\n  Safe alternative:\n  -- Safe enum recreation pattern:\n  -- 1. Create the new type:\n  --    CREATE TYPE status_new AS ENUM ('value1', 'value2');\n  -- 2. Migrate columns (ACCESS EXCLUSIVE lock, rewrites table):\n  --    ALTER TABLE <table> ALTER COLUMN <col>\n  --      TYPE status_new USING <col>::text::status_new;\n  -- 3. Drop the old type:\n  --    DROP TYPE status;\n  -- 4. Rename:\n  --    ALTER TYPE status_new RENAME TO status;\n\n  Why: Dropping an enum type destroys the type and fails if any columns reference it. PostgreSQL cannot remove individual enum values. You must recreate the type and migrate all columns, which requires an ACCESS EXCLUSIVE lock per table.\n  Docs: https://migrationpilot.dev/rules/mp024",
  MP025: "✗ [MP025] CRITICAL (line 2)\n  CONCURRENTLY operations cannot run inside a transaction block. PostgreSQL will raise: \"ERROR: CREATE INDEX CONCURRENTLY cannot run inside a transaction block\". Remove the surrounding BEGIN/COMMIT.\n\n  Safe alternative:\n  -- Remove the surrounding transaction block:\n  -- CONCURRENTLY operations manage their own locking.\n  CREATE INDEX CONCURRENTLY idx_users_email ON users (email)\n\n  Why: CONCURRENTLY operations cannot run inside a transaction block. PostgreSQL will raise a runtime ERROR, causing the entire migration to fail. Many migration frameworks wrap operations in transactions by default, making this a common trap.\n  Docs: https://migrationpilot.dev/rules/mp025",
  MP026: "✗ [MP026] CRITICAL (line 1)\n  DROP TABLE \"users\" permanently removes the table and all its data. This is irreversible and takes an ACCESS EXCLUSIVE lock.\n\n  Safe alternative:\n  -- Step 1: Rename the table (keeps data as backup)\n  ALTER TABLE users RENAME TO users_deprecated;\n  \n  -- Step 2: After confirming no application depends on it, drop later\n  -- DROP TABLE users_deprecated;\n\n  Why: DROP TABLE is irreversible. It permanently deletes the table, all rows, indexes, constraints, triggers, and policies. In production, this means instant data loss. Prefer renaming the table first, keeping it as a backup, then dropping later after confirming no dependencies.\n  Docs: https://migrationpilot.dev/rules/mp026",
  MP027: "✗ [MP027] CRITICAL (line 1)\n  Adding UNIQUE constraint \"uq_email\" on \"users\" scans the entire table under ACCESS EXCLUSIVE lock. Create the index concurrently first, then use USING INDEX.\n\n  Safe alternative:\n  -- Step 1: Create the unique index concurrently (non-blocking)\n  CREATE UNIQUE INDEX CONCURRENTLY uq_email_idx ON users (...);\n  \n  -- Step 2: Add the constraint using the pre-built index (instant)\n  ALTER TABLE users ADD CONSTRAINT uq_email UNIQUE USING INDEX uq_email_idx;\n\n  Why: ALTER TABLE ADD CONSTRAINT UNIQUE builds a unique index while holding ACCESS EXCLUSIVE lock, blocking all reads and writes for the entire scan. Instead, create the unique index concurrently (non-blocking), then attach it as a constraint with USING INDEX.\n  Docs: https://migrationpilot.dev/rules/mp027",
  MP028: "⚠ [MP028] WARNING (line 1)\n  Renaming table \"users\" to \"accounts\" will break all application queries, views, and foreign keys referencing the old name.\n\n  Safe alternative:\n  -- Step 1: Create a view with the old name pointing to the new table\n  -- (or use the expand-contract pattern):\n  -- CREATE VIEW users AS SELECT * FROM accounts;\n  \n  -- Step 2: Update application code to use \"accounts\"\n  -- Step 3: Drop the compatibility view after all code is updated\n  -- DROP VIEW users;\n\n  Why: Renaming a table takes an ACCESS EXCLUSIVE lock and instantly breaks every application query, view, function, foreign key, and trigger referencing the old name. Unlike renaming a column, there is no pg_attribute fallback. Use the create-copy-swap pattern for zero-downtime renames.\n  Docs: https://migrationpilot.dev/rules/mp028",
  MP029: "⚠ [MP029] WARNING (line 1)\n  Dropping NOT NULL on \"users\".\"email\" allows NULL values. Application code that assumes this column is always populated may break.\n\n  Safe alternative:\n  -- Verify that all application code handles NULL values for \"email\" before dropping NOT NULL.\n  -- Consider adding a default value instead:\n  -- ALTER TABLE users ALTER COLUMN email SET DEFAULT <value>;\n\n  Why: Dropping a NOT NULL constraint allows NULL values to be inserted into a column that was previously guaranteed non-null. Application code, ORMs, and downstream systems may crash or return incorrect results when encountering unexpected NULLs.\n  Docs: https://migrationpilot.dev/rules/mp029",
  MP030: "✗ [MP030] CRITICAL (line 1)\n  CHECK constraint \"chk_age\" on \"users\" without NOT VALID scans the entire table under ACCESS EXCLUSIVE lock, blocking all reads and writes.\n\n  Safe alternative:\n  -- Step 1: Add CHECK with NOT VALID (instant, no scan)\n  ALTER TABLE users ADD CONSTRAINT chk_age CHECK (...) NOT VALID;\n  \n  -- Step 2: Validate separately (SHARE UPDATE EXCLUSIVE, allows reads + writes)\n  ALTER TABLE users VALIDATE CONSTRAINT chk_age;\n\n  Why: ALTER TABLE ADD CONSTRAINT CHECK validates all existing rows while holding an ACCESS EXCLUSIVE lock, blocking all reads and writes. NOT VALID skips the scan during creation (instant), then VALIDATE CONSTRAINT checks rows under a less restrictive lock that allows concurrent reads and writes.\n  Docs: https://migrationpilot.dev/rules/mp030",
  MP031: "✗ [MP031] CRITICAL (line 1)\n  EXCLUSION constraint \"no_overlap\" on \"bookings\" builds a GiST index under ACCESS EXCLUSIVE lock. This cannot use NOT VALID and blocks all reads and writes for the full scan.\n\n  Safe alternative:\n  -- Exclusion constraints cannot be added without locking.\n  -- Consider these alternatives:\n  -- 1. Add during a maintenance window\n  -- 2. Create the table with the exclusion constraint from the start\n  -- 3. Use application-level uniqueness checking with advisory locks\n\n  Why: EXCLUSION constraints require a GiST index, which is built inline while holding ACCESS EXCLUSIVE lock. Unlike CHECK or FK constraints, exclusion constraints have no NOT VALID option: the full table scan and index build happen atomically, blocking all reads and writes for the entire duration.\n  Docs: https://migrationpilot.dev/rules/mp031",
  MP032: "✗ [MP032] CRITICAL (line 1)\n  CLUSTER on \"users\" USING \"idx_users_created\" rewrites the entire table under ACCESS EXCLUSIVE lock. This blocks ALL reads and writes for the entire duration.\n\n  Safe alternative:\n  -- Use pg_repack for online table reorganization (no ACCESS EXCLUSIVE lock):\n  -- Install: CREATE EXTENSION pg_repack;\n  -- Run: pg_repack --table users --order-by idx_users_created --no-superuser-check\n\n  Why: CLUSTER physically reorders all rows in the table to match an index, requiring a full table rewrite under ACCESS EXCLUSIVE lock. On large tables this can take hours, blocking all reads and writes. Use pg_repack for online table reorganization without blocking.\n  Docs: https://migrationpilot.dev/rules/mp032",
  MP033: "⚠ [MP033] WARNING (line 1)\n  REFRESH MATERIALIZED VIEW \"mv_user_stats\" without CONCURRENTLY takes ACCESS EXCLUSIVE lock, blocking all reads for the entire refresh duration.\n\n  Safe alternative:\n  -- Use CONCURRENTLY to allow reads during refresh (requires a UNIQUE index):\n  REFRESH MATERIALIZED VIEW CONCURRENTLY mv_user_stats;\n\n  Why: REFRESH MATERIALIZED VIEW without CONCURRENTLY takes an ACCESS EXCLUSIVE lock for the entire refresh duration, blocking all queries against the view. REFRESH CONCURRENTLY allows reads to continue using the old data while the new data is being computed. Requires a UNIQUE index on the materialized view.\n  Docs: https://migrationpilot.dev/rules/mp033",
  MP034: "✗ [MP034] CRITICAL (line 1)\n  DROP DATABASE \"production\" permanently destroys the entire database. This should never appear in a migration file.\n\n  Safe alternative:\n  -- DROP DATABASE should never be in a migration file.\n  -- If you need to reset a database, use a separate administrative script.\n\n  Why: DROP DATABASE permanently and irreversibly destroys the entire database including all tables, data, indexes, and extensions. If this statement appears in a migration file, it almost certainly indicates an error. There is no undo.\n  Docs: https://migrationpilot.dev/rules/mp034",
  MP035: "✗ [MP035] CRITICAL (line 1)\n  DROP SCHEMA \"analytics\" CASCADE permanently removes the schema and ALL objects within it. This is irreversible without a backup.\n\n  Safe alternative:\n  -- Verify the schema is empty and unused before dropping:\n  -- SELECT * FROM information_schema.tables WHERE table_schema = 'analytics';\n  -- SELECT * FROM information_schema.routines WHERE routine_schema = 'analytics';\n\n  Why: DROP SCHEMA removes the schema and, with CASCADE, all tables, views, functions, and types it contains. Even without CASCADE it takes an ACCESS EXCLUSIVE lock. Dropped schemas cannot be recovered without a backup restore.\n  Docs: https://migrationpilot.dev/rules/mp035",
  MP036: "✗ [MP036] CRITICAL (line 1)\n  TRUNCATE users CASCADE silently truncates all tables with foreign key references. This can destroy data across many related tables.\n\n  Safe alternative:\n  -- Truncate specific tables explicitly in dependency order:\n  -- TRUNCATE users;\n  -- Or use DELETE with WHERE clauses for safer, auditable data removal.\n\n  Why: TRUNCATE CASCADE removes all rows not only from the target table but from every table that references it via foreign keys, recursively. This can silently wipe data from dozens of tables. Always truncate specific tables explicitly.\n  Docs: https://migrationpilot.dev/rules/mp036",
  MP037: "⚠ [MP037] WARNING (line 1)\n  Column \"bio\" on \"users\" uses VARCHAR. In PostgreSQL, TEXT has identical performance. Use TEXT with a CHECK constraint if you need length validation.\n\n  Safe alternative:\n  -- Use TEXT instead of VARCHAR:\n  -- \"bio\" TEXT\n  -- If you need a max length, add a CHECK constraint:\n  -- \"bio\" TEXT CHECK (length(\"bio\") <= <max_length>)\n\n  Why: In PostgreSQL, VARCHAR(n) and TEXT have identical performance: both use the same varlena storage. VARCHAR(n) only adds a length check constraint that makes future length changes require a table rewrite (on PG < 17) or at minimum a constraint adjustment. Use TEXT with a CHECK constraint if you need length validation.\n  Docs: https://migrationpilot.dev/rules/mp037",
  MP038: "⚠ [MP038] WARNING (line 1)\n  Primary/foreign key column \"id\" on \"orders\" uses INT (max ~2.1B). Use BIGINT to avoid expensive future type migration.\n\n  Safe alternative:\n  -- Use BIGINT for primary/foreign key columns:\n  -- \"id\" BIGINT PRIMARY KEY\n\n  Why: INT primary keys overflow at ~2.1 billion rows. Migrating from INT to BIGINT on a large table requires a full table rewrite under ACCESS EXCLUSIVE lock, which can take hours. Starting with BIGINT avoids this expensive migration and costs only 4 extra bytes per row.\n  Docs: https://migrationpilot.dev/rules/mp038",
  MP039: "⚠ [MP039] WARNING (line 1)\n  Column \"id\" on \"users\" uses SERIAL. Use GENERATED ALWAYS AS IDENTITY instead (PG 10+, SQL-standard, cleaner ownership).\n\n  Safe alternative:\n  -- Use IDENTITY instead of SERIAL:\n  -- \"id\" BIGINT GENERATED ALWAYS AS IDENTITY\n\n  Why: SERIAL is a legacy shorthand that creates a separate sequence with confusing ownership semantics: dropping the column does not drop the sequence, and permissions are not automatically granted. GENERATED ALWAYS AS IDENTITY (PG 10+) is SQL-standard, has cleaner ownership, and is the recommended approach.\n  Docs: https://migrationpilot.dev/rules/mp039",
  MP040: "⚠ [MP040] WARNING (line 1)\n  Column \"created_at\" on \"events\" uses TIMESTAMP WITHOUT TIME ZONE. Use TIMESTAMPTZ to avoid timezone-related bugs.\n\n  Safe alternative:\n  -- Use TIMESTAMPTZ instead:\n  -- \"created_at\" TIMESTAMPTZ\n\n  Why: TIMESTAMP WITHOUT TIME ZONE stores the literal wall-clock time with no timezone context. When servers move between zones, or users are in different timezones, this causes silent data corruption. TIMESTAMPTZ stores instants in UTC and converts on display, which is almost always what you want.\n  Docs: https://migrationpilot.dev/rules/mp040",
  MP041: "⚠ [MP041] WARNING (line 1)\n  Column \"country_code\" on \"users\" uses CHAR(n), which blank-pads values and wastes storage. Use TEXT instead.\n\n  Safe alternative:\n  -- Use TEXT instead of CHAR(n):\n  -- \"country_code\" TEXT\n\n  Why: CHAR(n) blank-pads values to the specified length, wasting storage and causing confusing behavior in string comparisons and LIKE queries. It offers no performance advantage over TEXT in PostgreSQL. Use TEXT (or VARCHAR if you must have a length limit).\n  Docs: https://migrationpilot.dev/rules/mp041",
  MP042: "⚠ [MP042] WARNING (line 1)\n  CREATE INDEX on \"users\" without an explicit name. Auto-generated names are fragile and hard to reference in future migrations.\n\n  Safe alternative:\n  -- Add an explicit index name:\n  -- CREATE INDEX idx_users_<column> ON users (...);\n\n  Why: PostgreSQL auto-generates index names like \"users_email_idx\" but the naming is fragile and can collide. Explicit index names make future migrations clearer (DROP INDEX, REINDEX), improve monitoring (pg_stat_user_indexes), and are required for UNIQUE ... USING INDEX patterns.\n  Docs: https://migrationpilot.dev/rules/mp042",
  MP043: "⚠ [MP043] WARNING (line 1)\n  CREATE DOMAIN \"positive_int\" with CHECK constraint. Future columns using this domain will be validated against this constraint, and modifying it later requires scanning all tables using the domain.\n\n  Safe alternative:\n  -- Consider using CHECK constraints on individual columns instead of domain constraints.\n  -- This gives you more control over validation and avoids cross-table scans when modifying constraints.\n\n  Why: Domain constraints are validated against every column in every table that uses the domain type. Adding a CHECK constraint to a domain can trigger full table scans across many tables simultaneously. Use CHECK constraints on individual columns instead.\n  Docs: https://migrationpilot.dev/rules/mp043",
  MP044: "⚠ [MP044] WARNING (line 1)\n  Changing \"age\" on \"users\" to INT2 may cause data loss if existing values exceed the new type range. This also requires a full table rewrite.\n\n  Safe alternative:\n  -- Verify no data exceeds the new type range before changing:\n  -- SELECT COUNT(*) FROM users WHERE \"age\" > <max_value_of_new_type>;\n  -- Consider adding a CHECK constraint instead of narrowing the type.\n\n  Why: Narrowing a column type (e.g., BIGINT → INT, TEXT → VARCHAR(50)) will fail if any existing row has a value that does not fit the new type. Even if it succeeds today, future inserts may fail unexpectedly. The change also requires a full table rewrite under ACCESS EXCLUSIVE lock.\n  Docs: https://migrationpilot.dev/rules/mp044",
  MP045: "⚠ [MP045] WARNING (line 1)\n  CREATE TABLE \"events\" without a primary key. Tables without a PK cannot use logical replication and make row identification ambiguous.\n\n  Safe alternative:\n  -- Add a primary key column:\n  -- CREATE TABLE events (\n  --   id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  --   ...\n  -- );\n\n  Why: Tables without a primary key cannot use logical replication (a requirement for zero-downtime upgrades), make UPDATE/DELETE operations ambiguous, prevent efficient foreign key references, and break many ORMs. Add a primary key to every table.\n  Docs: https://migrationpilot.dev/rules/mp045",
  MP046: "✗ [MP046] CRITICAL (line 1)\n  DETACH PARTITION \"events_2024\" from \"events\" without CONCURRENTLY takes ACCESS EXCLUSIVE lock on the parent, blocking all queries.\n\n  Safe alternative:\n  -- Use CONCURRENTLY to avoid blocking (PG 14+):\n  ALTER TABLE events DETACH PARTITION events_2024 CONCURRENTLY;\n\n  Why: DETACH PARTITION without CONCURRENTLY takes an ACCESS EXCLUSIVE lock on the parent table, blocking all reads and writes until the operation completes. DETACH PARTITION CONCURRENTLY (PG 14+) uses a two-phase approach that only briefly locks the parent.\n  Docs: https://migrationpilot.dev/rules/mp046",
  MP047: "✗ [MP047] CRITICAL (line 1)\n  SET UNLOGGED on \"users\" rewrites the entire table under ACCESS EXCLUSIVE lock. This blocks all reads and writes for the full duration. UNLOGGED tables lose all data on crash.\n\n  Safe alternative:\n  -- Changing LOGGED/UNLOGGED requires a full table rewrite.\n  -- Consider performing this during a maintenance window.\n  -- For LOGGED→UNLOGGED: ensure you have backups, data will be lost on crash.\n  -- For UNLOGGED→LOGGED: consider creating a new LOGGED table and migrating data.\n\n  Why: Changing a table between LOGGED and UNLOGGED requires a full table rewrite while holding ACCESS EXCLUSIVE lock. On large tables this can take hours, blocking all queries. UNLOGGED tables also do not survive crash recovery: all data is lost on restart.\n  Docs: https://migrationpilot.dev/rules/mp047",
  MP048: "⚠ [MP048] WARNING (line 1)\n  Setting volatile default on \"users\".\"created_at\" only affects future INSERTs. Existing rows will NOT be updated. You may need a backfill UPDATE.\n\n  Safe alternative:\n  -- SET DEFAULT only affects new rows. To backfill existing rows:\n  -- 1. ALTER TABLE users ALTER COLUMN created_at SET DEFAULT <volatile_fn>;\n  -- 2. UPDATE users SET created_at = DEFAULT WHERE created_at IS NULL;\n  -- (Run the UPDATE in batches for large tables)\n\n  Why: ALTER TABLE ALTER COLUMN SET DEFAULT only affects future INSERTs. Existing rows are NOT updated. Setting a volatile function like now() or gen_random_uuid() as default may give the false impression that existing NULLs will be filled. You likely need a backfill UPDATE as well.\n  Docs: https://migrationpilot.dev/rules/mp048",
  MP049: "✗ [MP049] CRITICAL (line 1)\n  Partitioned table \"events\" primary key does not include partition key column(s): created_at. PostgreSQL requires all partition key columns in the primary key.\n\n  Safe alternative:\n  -- Include partition key columns in the primary key:\n  -- PRIMARY KEY (id, created_at)\n\n  Why: PostgreSQL requires that the primary key (and all unique constraints) on a partitioned table include all partition key columns. If omitted, the CREATE TABLE will fail at runtime. This ensures uniqueness can be enforced per-partition.\n  Docs: https://migrationpilot.dev/rules/mp049",
  MP050: "⚠ [MP050] WARNING (line 1)\n  Index \"idx_embeddings\" on \"items\" uses IVFFlat. HNSW provides better recall without needing training data or periodic reindexing.\n\n  Safe alternative:\n  -- Use HNSW instead of IVFFlat for better recall:\n  -- CREATE INDEX idx_embeddings ON items USING hnsw (embedding vector_cosine_ops);\n\n  Why: pgvector IVFFlat indexes need enough representative data at creation time to build good clusters. As data distribution changes, recall degrades and you must REINDEX. HNSW indexes build incrementally, have consistently better recall, and never need retraining. Prefer HNSW unless you have a specific reason to use IVFFlat.\n  Docs: https://migrationpilot.dev/rules/mp050",
  MP051: "⚠ [MP051] WARNING (line 1)\n  Table \"locations\" has spatial column(s) (geom) without a GIST index. Spatial queries will require full sequential scans.\n\n  Safe alternative:\n  -- Add a GIST index for efficient spatial queries:\n  CREATE INDEX CONCURRENTLY idx_locations_geom ON locations USING GIST (geom);\n\n  Why: PostGIS geometry and geography columns need a GIST or SP-GIST index for efficient spatial queries (ST_Contains, ST_DWithin, etc.). Without one, every spatial query triggers a full sequential scan. Adding the index at table creation time avoids the need for a later lock-heavy ALTER.\n  Docs: https://migrationpilot.dev/rules/mp051",
  MP052: "⚠ [MP052] WARNING (line 1)\n  Dropping column \"email\" from \"users\" may break views, functions, or triggers that reference it. Check dependencies before deploying.\n\n  Safe alternative:\n  -- Check for dependent objects before dropping:\n  SELECT dependent_ns.nspname || '.' || dependent_view.relname AS dependent_object,\n         dependent_view.relkind\n  FROM pg_depend\n  JOIN pg_rewrite ON pg_depend.objid = pg_rewrite.oid\n  JOIN pg_class AS dependent_view ON pg_rewrite.ev_class = dependent_view.oid\n  JOIN pg_namespace AS dependent_ns ON dependent_view.relnamespace = dependent_ns.oid\n  JOIN pg_class AS source_table ON pg_depend.refobjid = source_table.oid\n  JOIN pg_attribute ON pg_depend.refobjid = pg_attribute.attrelid AND pg_depend.refobjsubid = pg_attribute.attnum\n  WHERE source_table.relname = 'users' AND pg_attribute.attname = 'email';\n\n  Why: Views, functions, and triggers that reference a column will fail at query time, not at migration time, when the column is dropped, renamed, or its type is changed. PostgreSQL does not automatically update these dependent objects. This is the most common cause of post-deployment failures that static analysis cannot fully resolve without a database connection.\n  Docs: https://migrationpilot.dev/rules/mp052",
  MP053: "✗ [MP053] CRITICAL (line 2)\n  Migration file contains BEGIN without a matching COMMIT or ROLLBACK. This will leave an open transaction that holds locks indefinitely.\n\n  Safe alternative:\n  -- Add COMMIT at the end of the migration:\n  COMMIT;\n\n  Why: A migration with BEGIN but no COMMIT will either fail (if the migration runner auto-commits) or leave an open transaction that holds locks indefinitely. Conversely, a COMMIT without a preceding BEGIN is a no-op but indicates a structural problem. Always match BEGIN with COMMIT or ROLLBACK.\n  Docs: https://migrationpilot.dev/rules/mp053",
  MP054: "✗ [MP054] CRITICAL (line 3)\n  INSERT/UPDATE in the same transaction as ALTER TYPE \"status\" ADD VALUE 'archived'. The new enum value is not visible until COMMIT, so this statement will fail.\n\n  Safe alternative:\n  -- Split into separate transactions:\n  -- Transaction 1: ALTER TYPE status ADD VALUE 'archived';\n  -- Transaction 2 (after COMMIT): INSERT INTO events (status) VALUES ('archived')...\n\n  Why: On PostgreSQL < 12, ALTER TYPE ADD VALUE cannot run inside a transaction at all. On PG 12+, it can run in a transaction but the new enum value is not visible to other statements in the same transaction. Any INSERT or UPDATE referencing the new value will fail with \"unsafe use of new value.\" Prisma, TypeORM, and Alembic frequently generate migrations that trigger this.\n  Docs: https://migrationpilot.dev/rules/mp054",
  MP055: "✗ [MP055] CRITICAL (line 1)\n  Dropping primary key constraint \"users_pkey\" on \"users\" without setting REPLICA IDENTITY. This breaks logical replication (Supabase, Neon, RDS read replicas, CDC pipelines).\n\n  Safe alternative:\n  -- Before dropping the PK, set replica identity:\n  ALTER TABLE users REPLICA IDENTITY FULL;\n  -- Or create a unique index and use it:\n  -- CREATE UNIQUE INDEX CONCURRENTLY idx_users_new_pk ON users (id);\n  -- ALTER TABLE users REPLICA IDENTITY USING INDEX idx_users_new_pk;\n\n  Why: The default replica identity IS the primary key. When you drop a PK without setting REPLICA IDENTITY FULL or USING INDEX, all subsequent UPDATE and DELETE operations fail on logical replication subscribers (Supabase, Neon, AWS RDS read replicas, Debezium CDC). The publisher succeeds but the subscriber errors with \"cannot delete from table because it does not have a replica identity.\"\n  Docs: https://migrationpilot.dev/rules/mp055",
  MP056: "⚠ [MP056] WARNING (line 1)\n  GIN index \"idx_events_data\" on \"events\"(data) uses bare column reference. This index won't speed up ->> or -> queries, only @>, ?, ?|, ?& containment operators.\n\n  Safe alternative:\n  -- For ->> queries, use an expression B-tree index instead:\n  -- CREATE INDEX CONCURRENTLY idx_events_data ON events ((data->>'key'));\n  -- For @> containment queries, add jsonb_path_ops:\n  -- CREATE INDEX CONCURRENTLY idx_events_data ON events USING GIN (data jsonb_path_ops);\n\n  Why: A GIN index with default jsonb_ops on a bare JSONB column does NOT speed up queries using ->> or ->. Most ORMs (Prisma, TypeORM, Knex) generate WHERE metadata->>'key' = 'value' queries, which will still do a sequential scan. Use an expression B-tree index on the specific path instead, or use jsonb_path_ops if you only use @> containment queries.\n  Docs: https://migrationpilot.dev/rules/mp056",
  MP057: "✗ [MP057] CRITICAL (line 1)\n  ENABLE ROW LEVEL SECURITY on \"users\" without a matching CREATE POLICY. With no policies, all non-superuser queries return zero rows, silently breaking the application.\n\n  Safe alternative:\n  -- Always create at least one policy when enabling RLS:\n  ALTER TABLE users ENABLE ROW LEVEL SECURITY;\n  CREATE POLICY users_select ON users FOR SELECT USING (true);\n  -- Then add restrictive policies as needed.\n\n  Why: When RLS is enabled with zero policies, the default behavior is a complete deny: all queries from non-superuser roles return zero rows. No error is raised. SELECT * FROM table returns empty. This silently breaks applications in a way that looks like an empty table rather than a permission error. Supabase documents this as the leading cause of data exposure/lockout incidents.\n  Docs: https://migrationpilot.dev/rules/mp057",
  MP058: "⚠ [MP058] WARNING (line 1)\n  2 separate ALTER TABLE statements on \"users\". Each acquires ACCESS EXCLUSIVE lock independently. Combine into a single ALTER TABLE with multiple subcommands to reduce lock acquisitions from 2 to 1.\n\n  Safe alternative:\n  -- Combine into a single statement:\n  -- ALTER TABLE users\n  --   ADD COLUMN ...,\n  --   ALTER COLUMN ...,\n  --   ADD CONSTRAINT ...;\n\n  Why: Each ALTER TABLE acquires ACCESS EXCLUSIVE lock independently. Multiple separate ALTER TABLE statements on the same table means multiple lock/unlock cycles, each going through the lock queue. Long-running queries must finish before each lock acquisition. Combining subcommands into a single ALTER TABLE reduces the blocking window from N separate lock cycles to one. This only holds for subcommands that are independent of each other: a NOT VALID constraint and its VALIDATE, or a SET NOT NULL and the CHECK constraint proving it, are deliberately kept apart, and merging them takes the scan they were written to avoid and puts it back under ACCESS EXCLUSIVE.\n  Docs: https://migrationpilot.dev/rules/mp058",
  MP059: "⚠ [MP059] WARNING (line 1)\n  INSERT INTO \"users\" with explicit integer IDs without resetting the sequence. The next auto-generated ID may conflict with existing rows.\n\n  Safe alternative:\n  -- Reset the sequence after inserting with explicit IDs:\n  SELECT setval(pg_get_serial_sequence('users', 'id'), COALESCE(MAX(id), 1)) FROM users;\n\n  Why: When you seed data or migrate rows with explicit integer IDs, the sequence counter is not automatically updated. The next auto-generated INSERT picks a low ID that already exists, causing \"duplicate key violates unique constraint.\" This is one of the most common post-migration production errors, documented extensively in Django, Rails, Supabase, and Prisma issue trackers.\n  Docs: https://migrationpilot.dev/rules/mp059",
  MP060: "✗ [MP060] CRITICAL (line 1)\n  ALTER TYPE \"status\" RENAME VALUE is not replicated via logical replication. This silently causes enum mismatches on subscribers (Neon, Supabase, RDS replicas, CDC pipelines).\n\n  Safe alternative:\n  -- Instead of RENAME VALUE, use add-migrate-drop:\n  -- 1. ALTER TYPE status ADD VALUE 'new_name';\n  -- 2. UPDATE table SET col = 'new_name' WHERE col = 'old_name';\n  -- 3. -- Drop old value requires recreating the type (complex but safe)\n\n  Why: RENAME VALUE modifies the pg_enum catalog entry in-place. Logical replication does not replicate catalog changes, so subscribers (Neon branches, Supabase read replicas, RDS logical replicas, Debezium CDC) retain the old value name. Any row containing the renamed value that arrives via replication will fail to decode. The only safe alternative is to add a new value, migrate data, then drop the old one.\n  Docs: https://migrationpilot.dev/rules/mp060",
  MP061: "⚠ [MP061] WARNING (line 1)\n  Table \"users\": place fixed-size columns (id, age) before variable-length columns (name, bio) to reduce alignment padding.\n\n  Why: PostgreSQL stores columns in declaration order. Fixed-size types (int, bigint, timestamp, uuid) before variable-length types (text, jsonb, bytea) reduces alignment padding waste, saving 4-16 bytes per row on tables with mixed types.\n  Docs: https://migrationpilot.dev/rules/mp061",
  MP062: "✗ [MP062] CRITICAL (line 1)\n  Adding stored generated column \"full_name\" to \"users\" causes a full table rewrite. Use a regular column with a trigger, or a virtual generated column (PG 17+).\n\n  Why: ALTER TABLE ADD COLUMN with GENERATED ALWAYS AS ... STORED rewrites every row to compute and store the expression. On tables with millions of rows, this holds an ACCESS EXCLUSIVE lock for the entire rewrite, blocking all reads and writes.\n  Docs: https://migrationpilot.dev/rules/mp062",
  MP063: "⚠ [MP063] WARNING (line 1)\n  DO block contains DDL (ALTER TABLE, CREATE INDEX, ADD COLUMN) that cannot be statically analyzed for lock safety. Extract DDL into direct SQL statements.\n\n  Why: PL/pgSQL DO blocks execute arbitrary code that cannot be analyzed by SQL linters. DDL inside DO blocks (ALTER TABLE, CREATE INDEX, DROP) acquires the same locks as direct SQL, but the operations are invisible to static analysis. Extract DDL from DO blocks into direct SQL statements for full safety analysis.\n  Docs: https://migrationpilot.dev/rules/mp063",
  MP064: "✗ [MP064] CRITICAL (line 1)\n  DISABLE TRIGGER ALL on \"users\" breaks replication, audit logs, and FK enforcement. Use targeted trigger disabling or restructure the migration.\n\n  Why: ALTER TABLE DISABLE TRIGGER ALL/USER turns off all triggers on the table. This breaks logical replication (which uses triggers internally), disables audit logging triggers, and bypasses foreign key enforcement. If the session crashes before re-enabling triggers, they remain disabled permanently.\n  Docs: https://migrationpilot.dev/rules/mp064",
  MP065: "✗ [MP065] CRITICAL (line 1)\n  Explicit LOCK TABLE on users in ACCESS EXCLUSIVE mode. PostgreSQL DDL acquires locks automatically. Remove the explicit lock or restructure the migration.\n\n  Why: LOCK TABLE acquires an explicit lock that can block reads and writes. PostgreSQL DDL statements automatically acquire the correct lock. Explicit LOCK TABLE is rarely needed and often indicates a flawed migration strategy. High lock modes (EXCLUSIVE, ACCESS EXCLUSIVE) block all other operations.\n  Docs: https://migrationpilot.dev/rules/mp065",
  MP066: "⚠ [MP066] WARNING (line 1)\n  Table \"staging_data\" has autovacuum disabled. This causes table bloat and risks transaction ID wraparound. Re-enable autovacuum after bulk loading.\n\n  Why: Autovacuum prevents table bloat by reclaiming dead tuples, and prevents transaction ID wraparound, which can freeze the entire database. Disabling autovacuum is occasionally justified for temporary bulk-load staging tables, but is dangerous for any table that serves production traffic.\n  Docs: https://migrationpilot.dev/rules/mp066",
  MP067: "⚠ [MP067] WARNING (line 1)\n  DELETE on \"users\" without a WHERE clause removes every row in a single transaction. Use TRUNCATE for full deletes or batch with WHERE + LIMIT.\n\n  Safe alternative:\n  -- For full table delete, use TRUNCATE (much faster, minimal WAL):\n  TRUNCATE users;\n  \n  -- For partial deletes, batch to reduce lock duration:\n  DELETE FROM users WHERE ctid IN (\n    SELECT ctid FROM users LIMIT 10000\n  );\n\n  Why: A full-table DELETE generates a WAL entry for every row, bloats the table with dead tuples, and holds a ROW EXCLUSIVE lock for the entire duration. On tables with millions of rows, this can take hours, cause replication lag, and exhaust disk space. Use batched deletes or TRUNCATE instead.\n  Docs: https://migrationpilot.dev/rules/mp067",
  MP068: "⚠ [MP068] WARNING (line 1)\n  Sequence \"user_id_seq\" uses integer type (max ~2.1B). Use BIGINT to prevent overflow and avoid expensive future migration.\n\n  Safe alternative:\n  -- Use bigint for sequences to prevent overflow:\n  CREATE SEQUENCE user_id_seq AS bigint;\n\n  Why: Integer sequences overflow at ~2.1 billion (int4) or ~32,000 (int2). When a sequence overflows, all INSERTs fail with a \"nextval: reached maximum value\" error. Migrating from integer to bigint on a live sequence requires rewriting the dependent column under ACCESS EXCLUSIVE lock. Buildkite, Mailchimp, and others have reported major outages from this.\n  Docs: https://migrationpilot.dev/rules/mp068",
  MP069: "⚠ [MP069] WARNING (line 1)\n  FK \"fk_user\" locks both \"orders\" AND \"users\" simultaneously (SHARE ROW EXCLUSIVE). Set lock_timeout on both tables to fail fast.\n\n  Safe alternative:\n  -- Set a short lock_timeout to prevent cascading locks:\n  SET lock_timeout = '3s';\n  ALTER TABLE orders ADD CONSTRAINT fk_user\n    FOREIGN KEY (...) REFERENCES users (...) NOT VALID;\n  RESET lock_timeout;\n  \n  -- Then validate separately (weaker lock):\n  ALTER TABLE orders VALIDATE CONSTRAINT fk_user;\n\n  Why: ALTER TABLE ADD CONSTRAINT ... FOREIGN KEY acquires SHARE ROW EXCLUSIVE lock on both the table with the FK column AND the referenced table. This blocks writes to both tables simultaneously, doubling the blast radius. If either table is high-traffic, the lock wait can cascade to all queries on both tables.\n  Docs: https://migrationpilot.dev/rules/mp069",
  MP070: "⚠ [MP070] WARNING (line 1)\n  CREATE INDEX CONCURRENTLY \"idx_email\" on \"users\" without a preceding DROP INDEX IF EXISTS. If this fails and is retried, the stale invalid index will block creation.\n\n  Safe alternative:\n  -- Drop any stale invalid index before creating:\n  DROP INDEX CONCURRENTLY IF EXISTS idx_email;\n  CREATE INDEX CONCURRENTLY idx_email ON users (...);\n  \n  -- If a UNIQUE or PRIMARY KEY constraint owns idx_email, the drop is refused\n  -- outright. Rebuild it in place instead:\n  -- REINDEX INDEX CONCURRENTLY idx_email;\n\n  Why: If CREATE INDEX CONCURRENTLY fails (due to deadlock, unique violation, or timeout), it leaves behind an INVALID index that is never used for queries but still slows down writes. Retrying without first dropping the invalid index fails with \"relation already exists\" — or worse, with IF NOT EXISTS, silently succeeds without rebuilding it. Always precede CONCURRENTLY index creation with DROP INDEX IF EXISTS to handle retries safely. The exception is an index a UNIQUE or PRIMARY KEY constraint owns: PostgreSQL refuses to drop that one at all, so its retry path is REINDEX INDEX CONCURRENTLY, or dropping the constraint first.\n  Docs: https://migrationpilot.dev/rules/mp070",
  MP071: "⚠ [MP071] WARNING (line 1)\n  Renaming \"name\" to \"full_name\" on \"users\" will break any views, functions, or triggers referencing the old name. Use the add-copy-drop pattern instead.\n\n  Safe alternative:\n  -- Safe add-copy-drop pattern:\n  -- Step 1: Add new column\n  ALTER TABLE users ADD COLUMN full_name <type>;\n  \n  -- Step 2: Backfill data (batched for large tables)\n  UPDATE users SET full_name = name WHERE full_name IS NULL;\n  \n  -- Step 3: Update all views, functions, triggers to use \"full_name\"\n  \n  -- Step 4: Drop old column in a separate migration\n  ALTER TABLE users DROP COLUMN name;\n\n  Why: PostgreSQL does not automatically update views, functions, triggers, or policies when a column is renamed. All dependent objects continue referencing the old name and fail at query time. The safe alternative is the add-copy-drop pattern: add a new column, backfill data, update all dependents, then drop the old column in a separate migration.\n  Docs: https://migrationpilot.dev/rules/mp071",
  MP072: "⚠ [MP072] WARNING (line 1)\n  ATTACH PARTITION \"events_2024\" to \"events\" scans the DEFAULT partition under ACCESS EXCLUSIVE lock. Ensure the default partition is small or empty.\n\n  Safe alternative:\n  -- Before attaching, move rows from default partition to avoid a long scan:\n  -- 1. Create the new partition detached first\n  -- 2. Move matching rows from DEFAULT to new partition\n  -- 3. Then attach:\n  SET lock_timeout = '5s';\n  ALTER TABLE events ATTACH PARTITION events_2024 FOR VALUES ...;\n  RESET lock_timeout;\n\n  Why: When attaching a new partition, PostgreSQL must verify that no rows in the DEFAULT partition belong to the new partition range. This requires scanning the entire DEFAULT partition while holding an ACCESS EXCLUSIVE lock on it. If the default partition is large, this scan blocks all reads and writes for its entire duration.\n  Docs: https://migrationpilot.dev/rules/mp072",
  MP073: "✗ [MP073] CRITICAL (line 1)\n  ALTER SYSTEM requires SUPERUSER privileges and modifies postgresql.conf. Use ALTER DATABASE ... SET for per-database settings instead.\n\n  Safe alternative:\n  -- Use per-database settings instead of system-wide:\n  ALTER DATABASE mydb SET <parameter> = '<value>';\n\n  Why: Running migrations as SUPERUSER is a security risk: a malicious or buggy migration can modify system catalogs, bypass RLS, and damage the cluster. Managed database services (RDS, Cloud SQL, Neon, Supabase) do not grant SUPERUSER access, so these operations will fail in production. Design migrations to work with the minimum required privileges.\n  Docs: https://migrationpilot.dev/rules/mp073",
  MP074: "⚠ [MP074] WARNING (line 1)\n  FK \"fk_user\" on \"orders\" → \"users\" is not DEFERRABLE. This forces row-by-row checking during bulk operations.\n\n  Safe alternative:\n  ALTER TABLE orders ADD CONSTRAINT fk_user\n    FOREIGN KEY (...) REFERENCES users (...)\n    DEFERRABLE INITIALLY DEFERRED NOT VALID;\n\n  Why: Non-deferrable foreign keys are checked per-row during INSERT/UPDATE, requiring careful insertion order. With DEFERRABLE INITIALLY DEFERRED, FK checks happen at COMMIT time, allowing bulk inserts, circular references, and data migrations to proceed without ordering constraints. This is especially important for tables that reference each other.\n  Docs: https://migrationpilot.dev/rules/mp074",
  MP075: "⚠ [MP075] WARNING (line 1)\n  UPDATE on \"users\" with JSONB/JSON function calls. TOAST chunk rewrites cause table bloat until VACUUM runs. Run VACUUM after bulk updates.\n\n  Safe alternative:\n  -- After bulk TOAST-column updates, reclaim space:\n  UPDATE users SET metadata = jsonb_set(metadata, '{key}', '\"value\"')\n  \n  -- Run VACUUM to reclaim dead TOAST chunks:\n  VACUUM (VERBOSE) users;\n\n  Why: When you UPDATE a row with TOAST-stored columns, PostgreSQL creates new TOAST chunks and marks old chunks as dead. Dead chunks are only reclaimed by VACUUM. Bulk updates on TOAST-heavy columns can cause tables to grow many times their logical size, degrading query performance and exhausting disk space.\n  Docs: https://migrationpilot.dev/rules/mp075",
  MP076: "⚠ [MP076] WARNING (line 1)\n  SAVEPOINT \"my_savepoint\" creates a subtransaction that consumes a separate XID. In retry loops, this accelerates XID wraparound risk.\n\n  Safe alternative:\n  -- Consider restructuring to avoid subtransactions:\n  -- 1. Use separate transactions instead of SAVEPOINT/ROLLBACK TO\n  -- 2. Use advisory locks for coordination instead of subtransactions\n  -- 3. If retries are needed, retry the entire transaction, not a subtransaction\n\n  Why: Each SAVEPOINT allocates a new transaction ID (XID). In retry loops, every SAVEPOINT/ROLLBACK TO consumes another XID without the previous one being freed. On systems processing millions of transactions, subtransaction XID consumption can push the database toward XID wraparound, which forces a complete database freeze for maintenance. PostgreSQL 14+ improved subtransaction handling, but the XID cost remains.\n  Docs: https://migrationpilot.dev/rules/mp076",
  MP077: "⚠ [MP077] WARNING (line 1)\n  Column \"bio\" on \"users\" uses pglz compression. Use lz4 on PG 14+ for 3-5x faster compression/decompression.\n\n  Safe alternative:\n  ALTER TABLE users ALTER COLUMN bio SET COMPRESSION lz4;\n\n  Why: PostgreSQL 14 introduced lz4 as an alternative TOAST compression method. lz4 is 3-5x faster for both compression and decompression compared to the default pglz, with only slightly worse compression ratios. For JSONB, TEXT, and BYTEA columns with frequent reads/writes, lz4 significantly reduces CPU overhead.\n  Docs: https://migrationpilot.dev/rules/mp077",
  MP078: "⚠ [MP078] WARNING (line 1)\n  CREATE EXTENSION \"pgcrypto\" without VERSION clause. Pin the version for reproducible migrations across environments.\n\n  Safe alternative:\n  CREATE EXTENSION IF NOT EXISTS \"pgcrypto\" VERSION '1.0';\n\n  Why: Without a VERSION clause, CREATE EXTENSION installs the server's default version, which can differ between development, staging, and production environments. This makes migrations non-reproducible and can cause subtle behavior differences. Pinning the version ensures consistent behavior across all environments.\n  Docs: https://migrationpilot.dev/rules/mp078",
  MP079: "⚠ [MP079] WARNING (line 1)\n  RLS on \"posts\" has policies for select but missing insert, update, delete. Uncovered operations will be silently denied.\n\n  Safe alternative:\n  CREATE POLICY posts_insert ON posts FOR INSERT USING (true);\n  CREATE POLICY posts_update ON posts FOR UPDATE USING (true);\n  CREATE POLICY posts_delete ON posts FOR DELETE USING (true);\n\n  Why: When RLS is enabled, any operation without a policy is silently denied: queries return zero rows instead of raising an error. If you create a SELECT policy but forget INSERT, all inserts silently fail (or error on PG 15+). Always create policies for all operations or use a permissive ALL policy as a baseline.\n  Docs: https://migrationpilot.dev/rules/mp079",
  MP080: "⚠ [MP080] WARNING (line 2)\n  INSERT on \"settings\" in a DDL migration file. Separate schema changes and data changes into different migration files.\n\n  Safe alternative:\n  -- Move data manipulation to a separate migration file:\n  -- migrations/003_schema_change.sql (DDL only)\n  -- migrations/004_data_backfill.sql  (DML only, with batching)\n\n  Why: Mixing DDL (schema changes) and DML (data changes) in the same migration makes rollback harder, increases lock duration, and violates separation of concerns. If the DDL portion fails, the data changes may have already been applied. Data migrations should be in separate files with explicit rollback strategies and batching for large datasets.\n  Docs: https://migrationpilot.dev/rules/mp080",
  MP081: "⚠ [MP081] WARNING (line 2)\n  CHECK (email IS NOT NULL) NOT VALID on \"users\" can be simplified on PG18+. Use a native NOT NULL constraint added NOT VALID instead.\n\n  Safe alternative:\n  -- PG18+ native approach (simpler, same safety):\n  ALTER TABLE users ADD CONSTRAINT users_email_not_null\n    NOT NULL email NOT VALID;\n  ALTER TABLE users VALIDATE CONSTRAINT users_email_not_null;\n\n  Why: PostgreSQL 18 stores NOT NULL constraints in pg_constraint, so ALTER TABLE ... ADD CONSTRAINT ... NOT NULL col NOT VALID marks a column NOT NULL without scanning the table (instant), and VALIDATE CONSTRAINT checks the existing rows under a lock that allows reads and writes. The old workaround of adding a CHECK (col IS NOT NULL) NOT VALID constraint is no longer needed and adds unnecessary complexity.\n  Docs: https://migrationpilot.dev/rules/mp081",
  MP083: "⚠ [MP083] WARNING (line 1)\n  CREATE TABLE \"orders\" has FK constraint with non-deterministic collation. PG18 rejects FKs with non-deterministic collations.\n\n  Safe alternative:\n  -- Use a deterministic collation for FK columns:\n  -- Change COLLATE \"und-x-icu\" to COLLATE \"C\" or a deterministic locale\n\n  Why: PostgreSQL 18 now validates that foreign key columns use deterministic collations. Non-deterministic collations (like ICU case-insensitive) can cause FK lookups to match incorrect values, e.g. \"abc\" matching \"ABC\". PG18 rejects such FKs at creation time.\n  Docs: https://migrationpilot.dev/rules/mp083",
  MP084: "✗ [MP084] CRITICAL (line 1)\n  ADD COLUMN \"email\" NOT NULL on \"users\" has no DEFAULT. On a table that already has rows this aborts with: column \"email\" of relation \"users\" contains null values. It will pass on an empty CI database and fail in production.\n\n  Safe alternative:\n  -- Option A: give existing rows a value (PG 11+ does this without a rewrite):\n  ALTER TABLE users ADD COLUMN email <type> NOT NULL DEFAULT <value>;\n  \n  -- Option B: if there is no sensible default, add it nullable and tighten later:\n  ALTER TABLE users ADD COLUMN email <type>;\n  -- backfill in batches, then:\n  ALTER TABLE users ADD CONSTRAINT users_email_not_null\n    CHECK (email IS NOT NULL) NOT VALID;\n  ALTER TABLE users VALIDATE CONSTRAINT users_email_not_null;\n\n  Why: PostgreSQL has to give existing rows a value for the new column. Without a DEFAULT there is nothing to give them, so the statement fails with \"column ... contains null values\" and the whole migration rolls back. An empty CI database accepts the same statement happily, which is what makes this one dangerous: it passes every check you run before deploy and only fails in the environment that has data.\n  Docs: https://migrationpilot.dev/rules/mp084",
  MP085: "⚠ [MP085] WARNING (line 1)\n  GRANT ALL PRIVILEGES on users: grants ALL PRIVILEGES rather than the specific ones needed. Grant only the privileges the application actually uses, to a named role.\n\n  Safe alternative:\n  -- Grant the specific privileges to a named role instead:\n  GRANT SELECT, INSERT, UPDATE ON users TO app_role;\n  \n  -- If PUBLIC access was inherited from an older migration, revoke it explicitly:\n  REVOKE ALL ON users FROM PUBLIC;\n\n  Why: Privileges granted in a migration are permanent and rarely revisited. GRANT ... TO PUBLIC reaches every role in the cluster, including roles created years later, so it is the one grant that cannot be audited by listing current users. GRANT ALL hands over TRUNCATE and REFERENCES alongside the read access that was actually wanted. Blanket ON ALL TABLES IN SCHEMA grants apply to whatever happens to exist at that moment, which makes the resulting privilege set a function of migration ordering rather than intent.\n  Docs: https://migrationpilot.dev/rules/mp085",
  MP086: "⚠ [MP086] WARNING (line 1)\n  Foreign key \"fk_user\" on \"orders\" on column \"user_id\" → \"users\" has no ON DELETE clause, so it defaults to NO ACTION. Deleting a referenced row in \"users\" will fail once child rows exist. State the behaviour explicitly.\n\n  Safe alternative:\n  -- Spell out the intended behaviour:\n  --   NO ACTION / RESTRICT: refuse the parent delete (what you get today)\n  --   CASCADE:              delete the child rows too\n  --   SET NULL:             orphan the child rows\n  ALTER TABLE orders ADD CONSTRAINT fk_user\n    FOREIGN KEY (user_id) REFERENCES users (<column>)\n    ON DELETE RESTRICT\n    NOT VALID;\n  ALTER TABLE orders VALIDATE CONSTRAINT fk_user;\n\n  Why: The default is NO ACTION, so every attempt to delete a referenced parent row fails once the first child row exists. Teams usually discover this from a production error rather than from the migration, because the constraint behaves fine until someone deletes something. Writing the clause out, whether NO ACTION, RESTRICT, CASCADE, SET NULL, or SET DEFAULT, turns an invisible default into a reviewable decision, and CASCADE in particular deserves to be seen in review given it deletes rows in tables the migration never mentions.\n  Docs: https://migrationpilot.dev/rules/mp086",
  MP087: "✗ [MP087] CRITICAL (line 1)\n  CHECK constraint \"sessions_not_expired\" on \"sessions\" calls the volatile function now(). PostgreSQL evaluates it only at write time, so stored rows can stop satisfying it. They then become un-updatable, and restoring a dump fails with \"is violated by some row\".\n\n  Safe alternative:\n  -- Compare against a stored value rather than the current time:\n  ALTER TABLE sessions ADD CONSTRAINT sessions_not_expired\n    CHECK (expires_at > created_at) NOT VALID;\n  ALTER TABLE sessions VALIDATE CONSTRAINT sessions_not_expired;\n  \n  -- If the rule really is \"relative to now\", enforce it where it can be\n  -- re-evaluated: a partial index, a trigger, or the application, not in a\n  -- CHECK constraint that is frozen at insert time.\n\n  Why: PostgreSQL evaluates a CHECK constraint when a row is written and never again, so a predicate built on now() or random() stops describing the rows it admitted. Three things follow, none of them visible at migration time. The table quietly holds rows that violate its own constraint. Those rows become un-updatable: any UPDATE re-checks the constraint, so even writing to an unrelated column fails with \"new row violates check constraint\". And restoring a dump re-adds the constraint against the stored data, which fails with \"is violated by some row\", so the backup will not load. The constraint is not an invariant; it is a filter that was applied once.\n  Docs: https://migrationpilot.dev/rules/mp087",
  MP088: "⚠ [MP088] WARNING (line 1)\n  Backfill on \"users\" is not followed by ANALYZE. The planner will keep using pre-backfill statistics until autovacuum catches up, which can mean bad plans on \"users\" well after the migration reports success.\n\n  Safe alternative:\n  -- Refresh the statistics once the backfill is done:\n  ANALYZE users;\n  \n  -- ANALYZE takes only a ShareUpdateExclusiveLock, so it does not block\n  -- reads or writes and can run outside the migration transaction.\n\n  Why: After a backfill the statistics in pg_statistic still describe the table as it was before. The planner trusts them, so it keeps choosing plans built for the old null fraction and the old row count: sequential scans over a column that is now selective, nested loops sized for a fraction of the rows that are actually there. Autovacuum will fix it eventually, but it triggers off a row-change threshold rather than the end of your migration, so on a large table the gap is long enough to matter. A single ANALYZE closes it deterministically and takes a sample-sized fraction of the time the backfill just took.\n  Docs: https://migrationpilot.dev/rules/mp088",
  MP089: "⚠ [MP089] WARNING (line 1)\n  Changing \"users\".\"name\" to COLLATE \"en_US\" reorders the column. The table is rewritten and every index on the column is rebuilt, all under ACCESS EXCLUSIVE, and comparison, ORDER BY and LIKE results change afterwards.\n\n  Safe alternative:\n  -- Move the collation change off the critical path with expand-contract:\n  -- 1. Add the replacement column with the new collation\n  ALTER TABLE users ADD COLUMN name_new TEXT COLLATE \"en_US\";\n  -- 2. Backfill in batches, then build its indexes without blocking\n  CREATE INDEX CONCURRENTLY idx_users_name_new ON users (name_new);\n  -- 3. Swap the columns in a short transaction once the data is in place\n  \n  -- If you must change it in place, bound the lock wait:\n  SET lock_timeout = '5s';\n\n  Why: A collation defines the sort order, so changing it changes where every value belongs. Any btree index on the column is now ordered wrongly and has to be rebuilt, and that happens inside the same ACCESS EXCLUSIVE lock as the table rewrite rather than as separate work you can schedule. The behavioural change outlasts the migration: comparisons, ORDER BY, LIKE prefix matching and range queries on the column all answer differently afterwards, so a unique index can start rejecting values it used to accept and queries can silently return rows in a different order.\n  Docs: https://migrationpilot.dev/rules/mp089",
  MP090: "⚠ [MP090] WARNING (line 1)\n  CREATE TRIGGER \"audit_users\" on \"users\" is FOR EACH ROW on INSERT/DELETE/UPDATE. Creating it takes SHARE ROW EXCLUSIVE (writes block), and write_audit_log() then runs once per affected row inside every writing transaction on \"users\".\n\n  Safe alternative:\n  -- Bound the wait so the trigger creation cannot sit at the head of the\n  -- write queue behind a long-running transaction:\n  SET lock_timeout = '5s';\n  CREATE TRIGGER audit_users ...;\n  RESET lock_timeout;\n  \n  -- If the trigger only needs to see the change as a whole, a statement-level\n  -- trigger with transition tables runs once instead of once per row:\n  CREATE TRIGGER audit_users\n    AFTER UPDATE ON users\n    REFERENCING NEW TABLE AS changed\n    FOR EACH STATEMENT EXECUTE FUNCTION write_audit_log();\n\n  Why: Creating the trigger takes a SHARE ROW EXCLUSIVE lock, so writes queue behind it and, on a busy table, behind whatever long transaction is already holding a conflicting lock. That part is brief. The part that lasts is the trigger body: a row-level trigger executes once per affected row inside the writing transaction, so it is now on the critical path of every write to the table. A function that takes a millisecond turns a 10,000-row UPDATE into an extra ten seconds of held locks, and anything the trigger writes to is now part of that transaction too.\n  Docs: https://migrationpilot.dev/rules/mp090",
  MP091: "⚠ [MP091] WARNING (line 4)\n  GRANT on \"reports\" sits in a migration that also changes schema. Access-control changes reviewed as schema changes drift out of sight. Move privileges into their own migration.\n\n  Safe alternative:\n  -- Split the file so each change is reviewed as what it is:\n  -- migrations/012_add_reports_table.sql   (DDL only)\n  -- migrations/013_grant_reports_access.sql (GRANT/REVOKE only)\n\n  Why: A GRANT in the middle of a schema migration is a permanent access-control change reviewed as though it were a schema change. Nobody can answer \"who can read this table and who approved that\" without replaying every migration in order, because that history is the only record. Rolling back the schema change does not roll back the privilege either: DDL rollbacks restore structure, not the grants that came with it. Keeping privileges in their own migrations gives the access model one reviewable home.\n  Docs: https://migrationpilot.dev/rules/mp091",
  MP092: "⚠ [MP092] WARNING (line 3)\n  CREATE INDEX \"idx_events_id\" on partitioned table \"events\" recursively builds an index on every partition, holding locks across the whole hierarchy until the last one completes.\n\n  Safe alternative:\n  -- Build the parent index as a catalog-only placeholder, then fill it in\n  -- one partition at a time so each build stays online:\n  CREATE INDEX idx_events_id ON ONLY events (<columns>);\n  \n  -- For each partition:\n  CREATE INDEX CONCURRENTLY idx_events_id_p1 ON events_p1 (<columns>);\n  ALTER INDEX idx_events_id ATTACH PARTITION idx_events_id_p1;\n  \n  -- The parent index becomes valid once every partition index is attached.\n\n  Why: PostgreSQL rejects CREATE INDEX CONCURRENTLY on a partitioned table outright, so the habit that keeps index builds online everywhere else fails here, and it fails at run time, after the migration has started. The plain form is accepted but builds an index on every partition in one statement, holding locks across the whole hierarchy until the last partition finishes; on a table partitioned by month over three years that is thirty-six index builds inside one lock window. Building the parent index ON ONLY first, then each partition concurrently, keeps every individual build online and lets you stop between partitions.\n  Docs: https://migrationpilot.dev/rules/mp092",
  MP093: "⚠ [MP093] WARNING (line 1)\n  \"events_default\" is a DEFAULT partition of \"events\". It will silently absorb every row that matches no other partition, and each later ATTACH PARTITION on \"events\" must scan it in full under ACCESS EXCLUSIVE.\n\n  Safe alternative:\n  -- Prefer creating partitions ahead of time so a missing one fails loudly:\n  CREATE TABLE events_2026_01 PARTITION OF events\n    FOR VALUES FROM ('2026-01-01') TO ('2026-02-01');\n  \n  -- If you keep the default partition, monitor it so it stays near-empty:\n  SELECT count(*) FROM events_default;\n  \n  -- and drain it before attaching the partition its rows belong to, so the\n  -- ATTACH scan has nothing to find.\n\n  Why: The default partition turns a missing partition from a loud failure into a silent one. Without it, an insert with no matching partition raises an error somebody notices the same day; with it, the row lands in the catch-all and the gap goes unnoticed until the default partition is the largest table in the database. Getting out is the expensive part: attaching the partition those rows belonged to requires a full scan of the default partition under ACCESS EXCLUSIVE to prove no row overlaps the new bound, and if one does, the ATTACH fails and the rows have to be moved out by hand first.\n  Docs: https://migrationpilot.dev/rules/mp093",
  MP094: "⚠ [MP094] WARNING (line 1)\n  ATTACH PARTITION \"events_2026_01\" onto \"events\" has no matching CHECK constraint on \"events_2026_01\", so PostgreSQL scans the whole table to validate the bound while holding ACCESS EXCLUSIVE on both tables.\n\n  Safe alternative:\n  -- Add a CHECK matching the partition bound first. PostgreSQL then trusts\n  -- it and skips the validation scan during ATTACH.\n  ALTER TABLE events_2026_01\n    ADD CONSTRAINT events_2026_01_bound\n    CHECK (partition_key >= '<lower>' AND partition_key < '<upper>') NOT VALID;\n  \n  -- VALIDATE takes only SHARE UPDATE EXCLUSIVE, so reads and writes continue:\n  ALTER TABLE events_2026_01 VALIDATE CONSTRAINT events_2026_01_bound;\n  \n  -- Now the ATTACH is a catalog-only operation:\n  ALTER TABLE events ATTACH PARTITION events_2026_01 FOR VALUES ...;\n  \n  -- The CHECK is redundant once attached and can be dropped afterwards.\n\n  Why: Attaching a partition is meant to be a catalog operation, and it is, except for the validation scan. PostgreSQL reads every row of the incoming table to confirm it fits the partition bound, holding ACCESS EXCLUSIVE on both that table and the parent, which means the entire partitioned table is unavailable for the duration. On the multi-million-row table that a new partition usually is, that is minutes of downtime for what should be an instant operation. An equivalent CHECK constraint added beforehand lets PostgreSQL skip the scan on the strength of the constraint, and the work of validating it happens under a lock that does not block traffic.\n  Docs: https://migrationpilot.dev/rules/mp094",
  MP095: "⚠ [MP095] WARNING (line 1)\n  Table \"users\" is being moved to tablespace \"fast_ssd\". This copies every file under ACCESS EXCLUSIVE. \"users\" is unavailable for reads and writes until the copy finishes, and both copies occupy disk until it commits.\n\n  Safe alternative:\n  -- Move storage outside the migration, during a planned window, with a\n  -- bounded lock wait so it fails fast instead of queueing behind traffic:\n  SET lock_timeout = '5s';\n  ALTER TABLE users SET TABLESPACE fast_ssd;\n  RESET lock_timeout;\n  \n  -- For an index, rebuilding on the target tablespace keeps the old one\n  -- serving queries until the new index is ready:\n  -- CREATE INDEX CONCURRENTLY users_new ON <table> (<columns>) TABLESPACE fast_ssd;\n  -- DROP INDEX CONCURRENTLY users;\n  \n  -- Check free space on the destination before starting: both copies coexist.\n\n  Why: This is a physical file copy, not a catalog update. PostgreSQL holds ACCESS EXCLUSIVE from the first byte to the last, so the table is unavailable for reads and writes for as long as the copy takes: on a 500 GB table over ordinary disks, hours. Both copies exist until the move commits, so the destination needs the full size free and the source cannot be reclaimed early. A migration is the wrong place for it: there is no way to pause, resume, or bound the work, and cancelling midway rolls the whole copy back and leaves you where you started.\n  Docs: https://migrationpilot.dev/rules/mp095",
  MP096: "⚠ [MP096] WARNING (line 1)\n  CREATE MATERIALIZED VIEW \"daily_revenue\" populates immediately (WITH DATA is the default). The migration blocks until the full query completes, holding locks on every source table.\n\n  Safe alternative:\n  -- Create the view empty so the migration returns straight away:\n  CREATE MATERIALIZED VIEW daily_revenue AS SELECT ... WITH NO DATA;\n  \n  -- Populate it outside the migration. The first refresh cannot use\n  -- CONCURRENTLY, and the view is not queryable until it completes:\n  REFRESH MATERIALIZED VIEW daily_revenue;\n  \n  -- A unique index lets every later refresh run without blocking readers:\n  CREATE UNIQUE INDEX CONCURRENTLY daily_revenue_pk ON daily_revenue (<key>);\n  REFRESH MATERIALIZED VIEW CONCURRENTLY daily_revenue;\n\n  Why: WITH DATA is the default, so this usually happens without anyone choosing it. The migration then runs the view query to completion. An aggregate over a large fact table can take many minutes, while holding locks on every table the query reads and keeping the migration transaction open the entire time. Deploy tooling with a timeout gives up partway and leaves the schema half-applied. Creating the view WITH NO DATA returns immediately and moves the expensive part into a REFRESH you can schedule, retry, and run outside the deploy.\n  Docs: https://migrationpilot.dev/rules/mp096",
  MP097: "✗ [MP097] CRITICAL (line 4)\n  DROP INDEX \"users_email_key\" targets the index behind a UNIQUE constraint. PostgreSQL rejects this with \"cannot drop index users_email_key because constraint users_email_key ... requires it\" and the migration aborts here. This migration puts it under constraint \"users_email_key\".\n\n  Safe alternative:\n  -- The index cannot outlive its constraint, and cannot be dropped while the\n  -- constraint exists. Decide which one you actually mean:\n  \n  -- To keep the constraint: leave the index alone, it is not redundant.\n  -- To rebuild it because a concurrent build left it invalid, do it in place:\n  REINDEX INDEX CONCURRENTLY users_email_key;\n  \n  -- To remove the guarantee, drop the constraint and let the index go with it:\n  ALTER TABLE users DROP CONSTRAINT users_email_key;\n  \n  -- To swap in a differently-built index without losing the guarantee:\n  CREATE UNIQUE INDEX CONCURRENTLY users_email_key_new ON users (<columns>);\n  ALTER TABLE users DROP CONSTRAINT users_email_key,\n    ADD CONSTRAINT users_email_key UNIQUE USING INDEX users_email_key_new;\n\n  Why: The index is owned by the constraint, so PostgreSQL rejects the DROP INDEX with \"cannot drop index ... because constraint ... requires it\" and the migration aborts at that statement. The usual next move, adding CASCADE, turns a failed migration into a data-integrity change, because it drops the constraint as well and takes every foreign key referencing it along too. Dropping a unique constraint also removes the only thing preventing duplicate rows, and re-adding it later means a full table scan that fails outright if duplicates appeared while it was gone.\n  Docs: https://migrationpilot.dev/rules/mp097",
  MP098: "⚠ [MP098] WARNING (line 1)\n  Table \"users\" is being moved from its current schema to \"archive\". Every schema-qualified reference to it breaks at commit, and unqualified references now resolve only for roles whose search_path includes \"archive\".\n\n  Safe alternative:\n  -- Give callers an overlap window instead of a hard cutover. Move the\n  -- object, then leave a view behind at the old path:\n  ALTER TABLE users SET SCHEMA archive;\n  CREATE VIEW users AS\n    SELECT * FROM archive.users;\n  \n  -- Ship the application change, confirm nothing reads the old path, then\n  -- drop the compatibility view in a later migration:\n  -- DROP VIEW users;\n  \n  -- Check what still references the old path before cutting over:\n  SELECT * FROM pg_depend WHERE refobjid = 'users'::regclass;\n\n  Why: Moving an object between schemas is a rename in every way that matters. Queries that named it as old_schema.object start failing with \"relation does not exist\" the moment the migration commits, and there is no deprecation window: the old path stops working at the same instant the new one starts. Unqualified references are worse, because whether they still resolve depends on each role's search_path, so the migration can succeed, your psql session can look fine, and the application can still be broken. Views and functions that reference the object follow it by dependency, but the SQL your application ships does not.\n  Docs: https://migrationpilot.dev/rules/mp098",
  MP099: "✗ [MP099] CRITICAL (line 1)\n  Function \"promote_user\" is SECURITY DEFINER with no SET search_path. The caller controls how unqualified names in the body resolve, so they can shadow the objects it reads and have it act on theirs with the owner's privileges.\n\n  Safe alternative:\n  -- Pin search_path on the function so the caller cannot influence it:\n  CREATE FUNCTION promote_user(...) RETURNS ...\n    LANGUAGE plpgsql\n    SECURITY DEFINER\n    SET search_path = pg_catalog, public\n  AS $$ ... $$;\n  \n  -- Then restrict who can call it: SECURITY DEFINER functions are\n  -- executable by PUBLIC by default:\n  REVOKE EXECUTE ON FUNCTION promote_user FROM PUBLIC;\n  GRANT EXECUTE ON FUNCTION promote_user TO app_role;\n  \n  -- Schema-qualifying every reference inside the body is worth doing as well;\n  -- the SET clause is the guarantee, qualification is the belt and braces.\n\n  Why: SECURITY DEFINER runs the body with the owner's privileges, and search_path decides what the unqualified names in that body point at. Leave it unpinned and the caller supplies it: they create a table that shadows one the function reads, put their schema first on the path, and the function does privileged work against their object instead of yours. Functions owned by a superuser or by the schema owner turn this into privilege escalation for anyone who can call them. PostgreSQL accepts the function without a word, so nothing surfaces until someone goes looking, and by then the function is deployed and callable.\n  Docs: https://migrationpilot.dev/rules/mp099",
  MP100: "⚠ [MP100] WARNING (line 4)\n  \"idx_users_tenant\" on \"users\" indexes (tenant_id), which is already the leading column of existing index \"idx_users_tenant_created\" (tenant_id, created_at). PostgreSQL can serve those lookups from \"idx_users_tenant_created\". The new index adds build time, disk, and write overhead without adding a lookup path.\n\n  Safe alternative:\n  -- \"idx_users_tenant_created\" already covers (tenant_id):\n  -- CREATE INDEX idx_users_tenant_created ON public.users USING btree (tenant_id, created_at)\n  \n  -- Confirm the planner agrees before adding another index:\n  EXPLAIN (BUFFERS) SELECT * FROM users WHERE tenant_id = $1;\n  \n  -- Keep the new index only if it differs in a way that matters: a smaller index\n  -- for a hot lookup, a different access method, or a different sort order.\n\n  Why: A B-tree index on (a) is already covered by an existing index on (a, b): PostgreSQL can use the leading columns of a composite index on their own. Creating the narrower index buys nothing and costs a full build, permanent disk, and extra work on every INSERT, UPDATE, and DELETE. Redundant indexes also slow down planning, since the planner considers each one.\n  Docs: https://migrationpilot.dev/rules/mp100",
  MP101: "⚠ [MP101] WARNING (line 2)\n  \"events\" is taking about 117 writes/sec (10,100,000 row writes over the last 24.0 h): 8,000,000 inserts, 2,000,000 updates, 100,000 deletes. Adding \"idx_events_type\" makes every one of those writes maintain another index. This build is not CONCURRENTLY, so writes are blocked on the table until it finishes.\n\n  Safe alternative:\n  -- Build without blocking writes, outside a transaction:\n  CREATE INDEX CONCURRENTLY idx_events_type ON events (...);\n  \n  -- Then confirm the index earns the write overhead it costs:\n  SELECT indexrelname, idx_scan, pg_size_pretty(pg_relation_size(indexrelid))\n  FROM pg_stat_user_indexes\n  WHERE relname = 'events'\n  ORDER BY idx_scan;\n  \n  -- Indexes that never appear in idx_scan are pure write tax. Drop them.\n\n  Why: An index is not free after it is built. Every INSERT and DELETE maintains it, and an UPDATE that touches an indexed column loses the heap-only-tuple optimisation, so it writes a new index entry as well. On a write-hot table that shows up as higher latency on the write path and more WAL. The build is also slower and more disruptive here than anywhere else: a plain CREATE INDEX blocks writes for its whole duration, and CONCURRENTLY has to keep up with everything committed while it runs.\n  Docs: https://migrationpilot.dev/rules/mp101",
  MP102: "⚠ [MP102] WARNING (line 2)\n  VACUUM FULL rewrites \"orders\", which is 429.5 GB today (heap, indexes, and TOAST). The rewrite writes a full second copy before releasing the original, so peak usage is about 859.0 GB and roughly 429.5 GB of free space is needed. MigrationPilot cannot read free space from this server. PostgreSQL has no function for it. Check the data volume yourself (df -h on $PGDATA) before running this.\n\n  Safe alternative:\n  -- Check free space on the data volume first:\n  --   df -h $(psql -tAc \"SHOW data_directory\")\n  \n  -- Confirm what the copy will cost:\n  SELECT pg_size_pretty(pg_total_relation_size('orders')) AS current_size,\n         pg_size_pretty(pg_total_relation_size('orders') * 2) AS peak_during_rewrite;\n  \n  -- pg_repack rebuilds the table without the ACCESS EXCLUSIVE lock, but it still\n  -- needs the same second copy, so the disk requirement does not change:\n  --   pg_repack --table=orders --no-superuser-check <dbname>\n\n  Why: VACUUM FULL, CLUSTER, and a rewriting ALTER TABLE do not edit the table in place. PostgreSQL builds a complete new copy, heap and indexes, and only drops the original once the new copy is committed. Peak usage is therefore roughly twice the current size. If the volume fills up partway through, the rewrite fails and rolls back, and you have paid the full lock duration for nothing.\n  Docs: https://migrationpilot.dev/rules/mp102",
  MP103: "⚠ [MP103] WARNING (line 2)\n  UPDATE on \"events\" (200,000,000 rows, 64.4 GB) with 2 streaming replicas connected. The whole change set goes through WAL and each replica replays it serially, so lag will grow for the duration and reads served from replicas will be stale until they catch up. 2 replication slots are defined, so WAL is retained on the primary until the replicas have consumed it.\n\n  Safe alternative:\n  -- Split the work and let replicas catch up between batches:\n  --   run one batch, then wait until lag is back under your threshold.\n  \n  -- Watch lag while it runs:\n  SELECT client_addr,\n         state,\n         pg_size_pretty(pg_wal_lsn_diff(pg_current_wal_lsn(), replay_lsn)) AS replay_lag\n  FROM pg_stat_replication;\n  \n  -- If reads are served from replicas, run this in a window where stale reads are\n  -- acceptable, and confirm max_slot_wal_keep_size is set so a lagging replica\n  -- cannot fill the primary's disk.\n\n  Why: A table rewrite, an index build, or a large backfill writes the whole change set to WAL. Standbys replay that WAL with a single startup process, so work the primary spread across many backends arrives serially. Replication lag grows for as long as the operation runs and for some time after; read replicas serve stale data meanwhile, and a failover during the lag window loses whatever has not been replayed. Replication slots make it worse in the other direction: if a replica cannot keep up, the primary retains WAL for it and the disk fills.\n  Docs: https://migrationpilot.dev/rules/mp103",
  MP104: "⚠ [MP104] WARNING (line 2)\n  CREATE INDEX on \"events\" covers 500,000,000 rows (85.9 GB). Expect roughly 4 min to 67 min, a wide range because build speed depends on key width, memory, parallel workers, and I/O. Writes to \"events\" are blocked for that whole window: this build is not CONCURRENTLY.\n\n  Safe alternative:\n  -- Give the build more memory and workers for this session:\n  SET maintenance_work_mem = '2GB';\n  SET max_parallel_maintenance_workers = 4;\n  \n  -- Build without blocking writes (outside a transaction):\n  CREATE INDEX CONCURRENTLY idx_events_ts ON events (...);\n  \n  -- Watch it while it runs:\n  SELECT phase, blocks_done, blocks_total, tuples_done, tuples_total\n  FROM pg_stat_progress_create_index;\n  \n  -- Afterwards, check nothing was left behind by a cancelled build:\n  SELECT indexrelid::regclass FROM pg_index WHERE NOT indisvalid;\n\n  Why: Build time scales with row count, and a long build is a long exposure. A plain CREATE INDEX holds a SHARE lock that blocks every write on the table until it finishes. CONCURRENTLY does not block writes, but it makes two passes over the table and holds a snapshot throughout, which keeps vacuum from cleaning up dead rows anywhere in the database, and if it fails or is cancelled it leaves an INVALID index behind that must be dropped and rebuilt.\n  Docs: https://migrationpilot.dev/rules/mp104",
  MP105: "⚠ [MP105] WARNING (line 2)\n  CREATE INDEX CONCURRENTLY is not supported on hypertables. TimescaleDB documents WITH (timescaledb.transaction_per_chunk) as the alternative. This statement targets hypertable \"metrics\".\n\n  Safe alternative:\n  -- Build chunk by chunk so only one chunk is blocked at a time:\n  CREATE INDEX idx_metrics_device ON metrics (...)\n    WITH (timescaledb.transaction_per_chunk);\n  \n  -- Note: this is not supported for CREATE UNIQUE INDEX, and if it fails partway\n  -- through, some chunks keep the index and the hypertable's index is marked invalid.\n  -- A unique index on a hypertable must also include all partitioning columns.\n\n  Why: A hypertable stores its data in chunks, each a real table. TimescaleDB applies schema changes to the hypertable and to every chunk, so an ALTER that looks like one statement takes locks across the whole set and runs for as long as the slowest chunk. Index creation has its own catch: TimescaleDB does not support CREATE INDEX CONCURRENTLY on a hypertable, and offers WITH (timescaledb.transaction_per_chunk) instead, which builds chunk by chunk in separate transactions so only one chunk is blocked at a time.\n  Docs: https://migrationpilot.dev/rules/mp105",
  MP106: "⚠ [MP106] WARNING (line 1)\n  DELETE on hypertable \"metrics\" (420 chunks) filters on \"time\", its time dimension, so this removes whole chunks the slow way, one row at a time, leaving dead tuples for vacuum. drop_chunks() unlinks the chunks instead.\n\n  Safe alternative:\n  -- Drop the chunks that fall entirely outside the retention window:\n  SELECT drop_chunks('metrics', older_than => INTERVAL '30 days');\n  \n  -- drop_chunks only removes chunks whose whole range is outside the bound, so\n  -- data inside a partially-covered chunk stays. Check what would go first:\n  SELECT chunk_name, range_start, range_end\n  FROM timescaledb_information.chunks\n  WHERE hypertable_name = 'metrics'\n  ORDER BY range_end;\n  \n  -- For an ongoing policy rather than a one-off:\n  SELECT add_retention_policy('metrics', INTERVAL '30 days');\n\n  Why: Deleting old data from a hypertable row by row does the most expensive possible version of the job: a WAL record per row, a dead tuple per row for vacuum to clean up later, and bloat that survives until the vacuum finishes. Chunks are already partitioned on the time column, so the rows in a retention window are whole chunks. drop_chunks() drops those chunks as tables: no per-row work, no WAL per row, and the space comes back immediately.\n  Docs: https://migrationpilot.dev/rules/mp106",
  MP107: "⚠ [MP107] WARNING (line 2)\n  This ALTER targets \"tenant_id\", the distribution column of Citus table \"orders\". Citus refuses ALTER TABLE commands involving the distribution column, so the migration will fail rather than propagate.\n\n  Safe alternative:\n  -- See what this statement fans out to:\n  SELECT table_name, citus_table_type, distribution_column, shard_count\n  FROM citus_tables WHERE table_name::text LIKE '%orders%';\n  \n  -- If parallel propagation is deadlocking against other work, serialise it:\n  BEGIN;\n  SET LOCAL citus.multi_shard_modify_mode TO 'sequential';\n  SET LOCAL lock_timeout = '5s';\n  ALTER TABLE orders ALTER COLUMN tenant_id TYPE bigint\n  COMMIT;\n  \n  -- Changing the distribution column is not an ALTER. It means redistributing the\n  -- table (undistribute_table + create_distributed_table, or alter_distributed_table).\n\n  Why: A distributed table is a set of shards spread across worker nodes. Citus propagates DDL to all of them, so the ALTER holds a lock on the coordinator while each worker locks its own shards, and it is only done when the slowest worker is done. Anything that blocks on the coordinator blocks the whole cluster for that table. Some forms are refused outright rather than propagated: changing the distribution column is the common one.\n  Docs: https://migrationpilot.dev/rules/mp107",
  MP108: "⚠ [MP108] WARNING (line 2)\n  CREATE TABLE events_p2024_01 PARTITION OF on \"events\", which pg_partman manages (control column \"created_at\", interval 1 day, premake 4, retention 90 days). partman creates and removes children from part_config on its own schedule; a partition changed by hand is not recorded there, so the next run_maintenance can collide with it. Its retention policy is 90 days, so partman will drop children on that schedule whether or not this migration made them.\n\n  Safe alternative:\n  -- Check what partman thinks it owns before changing anything:\n  SELECT parent_table, control, partition_interval, premake, retention, automatic_maintenance\n  FROM part_config WHERE parent_table LIKE '%events';\n  \n  -- Let partman make the partitions, by widening how far ahead it pre-creates:\n  UPDATE part_config SET premake = 8 WHERE parent_table = 'public.events';\n  CALL run_maintenance_proc();\n  \n  -- To move existing rows into the partition set, use partman's own procedure so\n  -- it commits in batches instead of one long transaction:\n  CALL partition_data_proc('public.events');\n  \n  -- If the partition set really should be managed by hand from now on, take it out\n  -- of partman first: DELETE FROM part_config WHERE parent_table = '...';\n\n  Why: pg_partman decides which children exist from part_config: run_maintenance pre-creates the next few partitions and applies the retention policy. A partition created, attached, or detached by hand is invisible to that bookkeeping, so the next maintenance run can try to create a partition whose range you already covered, which fails, or drop one it believes it owns. The two systems disagree quietly and the failure surfaces later, on a maintenance run nobody was watching.\n  Docs: https://migrationpilot.dev/rules/mp108",
  MP109: "⚠ [MP109] WARNING (line 1)\n  HNSW index \"idx_items_embedding\" on \"items\" sets no m or ef_construction, so it builds with pgvector's defaults (m = 16, ef_construction = 64). Those choose the graph's recall ceiling, and changing them later means a full rebuild.\n\n  Safe alternative:\n  -- Set the graph parameters deliberately:\n  CREATE INDEX idx_items_embedding ON items\n    USING hnsw (embedding vector_cosine_ops)\n    WITH (m = 16, ef_construction = 128);\n  \n  -- Higher ef_construction means better recall for a slower build; m raises both\n  -- recall and index size. Measure recall against a known-good result set before\n  -- committing to the values, because changing them means rebuilding.\n  \n  -- ef_search is a query-time knob, not a build one. Tune it separately:\n  SET hnsw.ef_search = 100;\n\n  Why: pgvector index parameters are build-time decisions. For HNSW, m and ef_construction set the shape of the graph and therefore the recall ceiling; the defaults (m = 16, ef_construction = 64) are conservative and are frequently too low for production recall targets. For IVFFlat, lists decides how the vectors are clustered, and pgvector ties the right value to the row count: rows / 1000 up to a million rows, sqrt(rows) beyond that. Changing any of them later means rebuilding the whole index, which on a vector table is one of the most expensive builds in PostgreSQL.\n  Docs: https://migrationpilot.dev/rules/mp109",
  MP110: "⚠ [MP110] WARNING (line 2)\n  ALTER TABLE on partitioned table \"events\", which has 365 partitions. PostgreSQL takes the ACCESS EXCLUSIVE lock on the parent and on all 365 partitions and holds them until the statement commits, so the real blocking window is set by the slowest partition, not by the one line in this file.\n\n  Safe alternative:\n  -- Confirm the fan-out before running it:\n  SELECT count(*) AS partitions FROM pg_inherits WHERE inhparent = 'events'::regclass;\n  \n  -- Make sure the lock table can hold one entry per partition:\n  SHOW max_locks_per_transaction;\n  \n  -- Fail fast instead of stalling every query queued behind the parent lock:\n  SET lock_timeout = '5s';\n  ALTER TABLE events ADD COLUMN note text\n  RESET lock_timeout;\n  \n  -- For an index, build on each partition first, then attach them to a parent\n  -- index created ONLY, which keeps each blocking window to a single partition:\n  --   CREATE INDEX CONCURRENTLY idx_part_1 ON events_p1 (col);\n  --   CREATE INDEX idx_parent ON ONLY events (col);\n  --   ALTER INDEX idx_parent ATTACH PARTITION idx_part_1;\n\n  Why: DDL on a partitioned table recurses. PostgreSQL locks the parent and each partition, and it holds all of those locks until the statement commits, so the blocking window is set by the slowest partition and the number of locks by the partition count. Two things follow: the statement can exhaust max_locks_per_transaction on a table with many partitions, and a lock_timeout only helps if it is short enough to fire before the queue behind the parent lock has stalled every query.\n  Docs: https://migrationpilot.dev/rules/mp110",
  MP111: "✗ [MP111] CRITICAL (line 2)\n  ALTER COLUMN ... TYPE on \"value\" against \"metrics\", a hypertable with the columnstore enabled. TimescaleDB rejects this with \"operation not supported on hypertables that have columnstore enabled\". The migration will fail here, not run slowly.\n\n  Safe alternative:\n  -- Confirm the columnstore state first:\n  SELECT hypertable_name, compression_enabled, num_chunks\n  FROM timescaledb_information.hypertables\n  WHERE hypertable_name = 'metrics';\n  \n  -- Getting this change through means unwinding the columnstore and putting it back:\n  --   1. remove the columnstore policy for metrics\n  --   2. convert the compressed chunks back to rowstore\n  --   3. disable the columnstore on the hypertable\n  --   4. run: ALTER TABLE metrics ALTER COLUMN value TYPE numeric\n  --   5. re-enable the columnstore and restore the policy\n  --\n  -- Steps 2 and 5 move all of the hypertable's data, so schedule this as its own\n  -- maintenance operation rather than a step inside a migration run.\n\n  Why: TimescaleDB blocks several ALTER forms on a hypertable whose chunks are in the columnstore, and answers with \"operation not supported on hypertables that have columnstore enabled\". This is not a slow path: the statement fails. Getting it through means stopping the columnstore policy, converting the chunks back to rowstore, disabling the columnstore, applying the change, then putting all of it back: a long, data-moving procedure that does not belong in the middle of an ordinary migration run.\n  Docs: https://migrationpilot.dev/rules/mp111",
  MP112: "⚠ [MP112] WARNING (line 2)\n  HNSW build \"idx_items_embedding\" on \"items\" covers 8,000,000 rows with maintenance_work_mem at 64MB. pgvector keeps the graph in that memory while it fits and drops to a much slower path once it does not. The notice reads \"hnsw graph no longer fits into maintenance_work_mem after N tuples\". max_parallel_maintenance_workers is 2, which also caps how much of the build can run in parallel.\n\n  Safe alternative:\n  -- Raise the limits for the session that builds the index:\n  SET maintenance_work_mem = '8GB';\n  SET max_parallel_maintenance_workers = 7;\n  \n  CREATE INDEX idx_items_embedding ON items\n    USING hnsw (embedding vector_cosine_ops) WITH (m = 16, ef_construction = 128);\n  \n  -- Watch the server log during the build: if the \"no longer fits\" notice appears,\n  -- the rest of the build is on the slow path and more memory would have helped.\n  \n  -- max_parallel_maintenance_workers is capped by max_parallel_workers, so raise\n  -- that too if you want more than a couple of workers on the build.\n\n  Why: pgvector builds the HNSW graph in maintenance_work_mem. While the graph fits, the build is fast; once it does not, pgvector logs \"hnsw graph no longer fits into maintenance_work_mem after N tuples\" and finishes on a much slower path. The statement is identical either way, so a build that took minutes in staging can run for hours in production purely because the setting is lower there. maintenance_work_mem can be raised for the session that runs the build, which is the cheapest fix available.\n  Docs: https://migrationpilot.dev/rules/mp112",
};

/** The "Related Rules" section of each rule's doc. */
export const relatedRulesByRuleId: Record<string, { id: string; name: string; note: string }[]> = {
  MP001: [
    { id: "MP009", name: "require-drop-index-concurrently", note: "DROP INDEX should also use CONCURRENTLY" },
    { id: "MP023", name: "require-if-not-exists", note: "indexes should use IF NOT EXISTS for idempotency" },
    { id: "MP042", name: "require-index-name", note: "indexes should have explicit names" },
  ],
  MP002: [
    { id: "MP018", name: "no-force-set-not-null", note: "similar pattern for enforcing NOT NULL safely" },
    { id: "MP030", name: "require-not-valid-check", note: "CHECK constraints should use NOT VALID" },
    { id: "MP004", name: "require-lock-timeout", note: "set lock_timeout before DDL" },
  ],
  MP003: [
    { id: "MP048", name: "ban-alter-default-volatile-existing", note: "setting volatile default on existing column" },
    { id: "MP015", name: "no-add-column-serial", note: "SERIAL creates implicit volatile default" },
    { id: "MP011", name: "unbatched-data-backfill", note: "backfills should be batched" },
  ],
  MP004: [
    { id: "MP020", name: "require-statement-timeout", note: "statement_timeout for long-running DDL" },
    { id: "MP019", name: "no-exclusive-lock-high-connections", note: "lock contention with many connections" },
    { id: "MP013", name: "high-traffic-table-ddl", note: "DDL on high-traffic tables" },
  ],
  MP005: [
    { id: "MP016", name: "require-index-on-fk", note: "FK columns should have an index" },
    { id: "MP030", name: "require-not-valid-check", note: "CHECK constraints should also use NOT VALID" },
    { id: "MP004", name: "require-lock-timeout", note: "set lock_timeout before DDL" },
  ],
  MP006: [
    { id: "MP032", name: "ban-cluster", note: "CLUSTER also rewrites the entire table" },
    { id: "MP020", name: "require-statement-timeout", note: "long-running operations need timeouts" },
    { id: "MP004", name: "require-lock-timeout", note: "set lock_timeout before DDL" },
  ],
  MP007: [
    { id: "MP044", name: "no-data-loss-type-narrowing", note: "narrowing types can cause data loss" },
    { id: "MP010", name: "no-rename-column", note: "renaming also uses expand-contract" },
    { id: "MP011", name: "unbatched-data-backfill", note: "backfills should be batched" },
  ],
  MP008: [
    { id: "MP025", name: "ban-concurrent-in-transaction", note: "CONCURRENTLY cannot run in transactions" },
    { id: "MP004", name: "require-lock-timeout", note: "set lock_timeout before each DDL" },
    { id: "MP001", name: "require-concurrent-index-creation", note: "indexes should use CONCURRENTLY" },
  ],
  MP009: [
    { id: "MP001", name: "require-concurrent-index-creation", note: "CREATE INDEX should also use CONCURRENTLY" },
    { id: "MP025", name: "ban-concurrent-in-transaction", note: "CONCURRENTLY cannot run in transactions" },
    { id: "MP004", name: "require-lock-timeout", note: "set lock_timeout before DDL" },
  ],
  MP010: [
    { id: "MP028", name: "no-rename-table", note: "renaming a table has similar risks" },
    { id: "MP007", name: "no-column-type-change", note: "type changes also use expand-contract" },
    { id: "MP017", name: "no-drop-column", note: "dropping columns requires careful coordination" },
  ],
  MP011: [
    { id: "MP003", name: "volatile-default-table-rewrite", note: "volatile defaults may need backfills" },
    { id: "MP007", name: "no-column-type-change", note: "type changes require batched backfills" },
    { id: "MP010", name: "no-rename-column", note: "column renames require data migration" },
  ],
  MP012: [
    { id: "MP024", name: "no-enum-value-removal", note: "DROP TYPE destroys enum and dependent columns" },
    { id: "MP025", name: "ban-concurrent-in-transaction", note: "other operations that fail inside transactions" },
    { id: "MP008", name: "no-multi-ddl-transaction", note: "avoid wrapping DDL in transactions" },
  ],
  MP013: [
    { id: "MP014", name: "large-table-ddl", note: "DDL on large tables has similar risks" },
    { id: "MP019", name: "no-exclusive-lock-high-connections", note: "lock contention with many connections" },
    { id: "MP004", name: "require-lock-timeout", note: "always set lock_timeout before DDL" },
  ],
  MP014: [
    { id: "MP013", name: "high-traffic-table-ddl", note: "DDL on high-traffic tables" },
    { id: "MP004", name: "require-lock-timeout", note: "set lock_timeout before DDL" },
    { id: "MP020", name: "require-statement-timeout", note: "set statement_timeout for long operations" },
  ],
  MP015: [
    { id: "MP039", name: "prefer-identity-over-serial", note: "SERIAL should be replaced by IDENTITY" },
    { id: "MP003", name: "volatile-default-table-rewrite", note: "volatile defaults and table rewrites" },
    { id: "MP038", name: "prefer-bigint-over-int", note: "use BIGINT for key columns" },
  ],
  MP016: [
    { id: "MP005", name: "require-not-valid-foreign-key", note: "FK should use NOT VALID pattern" },
    { id: "MP001", name: "require-concurrent-index-creation", note: "indexes should use CONCURRENTLY" },
    { id: "MP042", name: "require-index-name", note: "indexes should have explicit names" },
  ],
  MP017: [
    { id: "MP010", name: "no-rename-column", note: "renaming columns has similar application breakage risks" },
    { id: "MP026", name: "ban-drop-table", note: "dropping tables is even more destructive" },
    { id: "MP004", name: "require-lock-timeout", note: "set lock_timeout before DDL" },
  ],
  MP018: [
    { id: "MP002", name: "require-check-not-null-pattern", note: "same CHECK constraint pattern" },
    { id: "MP030", name: "require-not-valid-check", note: "CHECK constraints should use NOT VALID" },
    { id: "MP029", name: "ban-drop-not-null", note: "dropping NOT NULL can break application code" },
  ],
  MP019: [
    { id: "MP013", name: "high-traffic-table-ddl", note: "DDL on high-traffic tables" },
    { id: "MP004", name: "require-lock-timeout", note: "always set lock_timeout before DDL" },
    { id: "MP014", name: "large-table-ddl", note: "large tables amplify lock impact" },
  ],
  MP020: [
    { id: "MP004", name: "require-lock-timeout", note: "lock_timeout prevents queue buildup" },
    { id: "MP006", name: "no-vacuum-full", note: "VACUUM FULL is a long-running operation" },
    { id: "MP032", name: "ban-cluster", note: "CLUSTER is a long-running operation" },
  ],
  MP021: [
    { id: "MP001", name: "require-concurrent-index-creation", note: "CREATE INDEX should use CONCURRENTLY" },
    { id: "MP009", name: "require-drop-index-concurrently", note: "DROP INDEX should use CONCURRENTLY" },
    { id: "MP020", name: "require-statement-timeout", note: "set timeout for long operations" },
  ],
  MP022: [
    { id: "MP026", name: "ban-drop-table", note: "DROP TABLE itself is dangerous" },
    { id: "MP035", name: "ban-drop-schema", note: "DROP SCHEMA CASCADE is similar" },
    { id: "MP036", name: "ban-truncate-cascade", note: "TRUNCATE CASCADE destroys related data" },
  ],
  MP023: [
    { id: "MP001", name: "require-concurrent-index-creation", note: "indexes should use CONCURRENTLY" },
    { id: "MP042", name: "require-index-name", note: "indexes should have explicit names" },
    { id: "MP045", name: "require-primary-key", note: "tables should have a primary key" },
  ],
  MP024: [
    { id: "MP012", name: "no-enum-add-value-in-transaction", note: "adding enum values in transactions" },
    { id: "MP007", name: "no-column-type-change", note: "type changes require table rewrites" },
    { id: "MP022", name: "no-drop-cascade", note: "CASCADE can silently destroy dependents" },
  ],
  MP025: [
    { id: "MP001", name: "require-concurrent-index-creation", note: "CREATE INDEX should use CONCURRENTLY" },
    { id: "MP009", name: "require-drop-index-concurrently", note: "DROP INDEX should use CONCURRENTLY" },
    { id: "MP008", name: "no-multi-ddl-transaction", note: "avoid wrapping DDL in transactions" },
  ],
  MP026: [
    { id: "MP022", name: "no-drop-cascade", note: "CASCADE silently drops dependent objects" },
    { id: "MP034", name: "ban-drop-database", note: "DROP DATABASE is even more destructive" },
    { id: "MP017", name: "no-drop-column", note: "DROP COLUMN also needs careful coordination" },
  ],
  MP027: [
    { id: "MP001", name: "require-concurrent-index-creation", note: "indexes should use CONCURRENTLY" },
    { id: "MP030", name: "require-not-valid-check", note: "CHECK constraints should use NOT VALID" },
    { id: "MP004", name: "require-lock-timeout", note: "set lock_timeout before DDL" },
  ],
  MP028: [
    { id: "MP010", name: "no-rename-column", note: "renaming columns has similar risks" },
    { id: "MP026", name: "ban-drop-table", note: "dropping tables is irreversible" },
    { id: "MP017", name: "no-drop-column", note: "dropping columns breaks running code" },
  ],
  MP029: [
    { id: "MP002", name: "require-check-not-null-pattern", note: "safe pattern for adding NOT NULL" },
    { id: "MP018", name: "no-force-set-not-null", note: "SET NOT NULL needs CHECK constraint pattern" },
    { id: "MP017", name: "no-drop-column", note: "column changes can break application code" },
  ],
  MP030: [
    { id: "MP005", name: "require-not-valid-foreign-key", note: "FK constraints should also use NOT VALID" },
    { id: "MP002", name: "require-check-not-null-pattern", note: "NOT NULL via CHECK pattern" },
    { id: "MP031", name: "ban-exclusion-constraint", note: "exclusion constraints cannot use NOT VALID" },
  ],
  MP031: [
    { id: "MP027", name: "disallowed-unique-constraint", note: "UNIQUE constraints should use USING INDEX" },
    { id: "MP030", name: "require-not-valid-check", note: "CHECK constraints should use NOT VALID" },
    { id: "MP004", name: "require-lock-timeout", note: "set lock_timeout before DDL" },
  ],
  MP032: [
    { id: "MP006", name: "no-vacuum-full", note: "VACUUM FULL also rewrites the entire table" },
    { id: "MP020", name: "require-statement-timeout", note: "long-running operations need timeouts" },
    { id: "MP004", name: "require-lock-timeout", note: "set lock_timeout before DDL" },
  ],
  MP033: [
    { id: "MP001", name: "require-concurrent-index-creation", note: "indexes should use CONCURRENTLY" },
    { id: "MP009", name: "require-drop-index-concurrently", note: "DROP INDEX should use CONCURRENTLY" },
    { id: "MP020", name: "require-statement-timeout", note: "long-running operations need timeouts" },
  ],
  MP034: [
    { id: "MP035", name: "ban-drop-schema", note: "DROP SCHEMA is similarly destructive" },
    { id: "MP026", name: "ban-drop-table", note: "DROP TABLE destroys individual tables" },
    { id: "MP022", name: "no-drop-cascade", note: "CASCADE amplifies destructive operations" },
  ],
  MP035: [
    { id: "MP034", name: "ban-drop-database", note: "DROP DATABASE is even more destructive" },
    { id: "MP026", name: "ban-drop-table", note: "DROP TABLE on individual tables" },
    { id: "MP022", name: "no-drop-cascade", note: "CASCADE silently drops dependent objects" },
  ],
  MP036: [
    { id: "MP022", name: "no-drop-cascade", note: "CASCADE on DROP is equally dangerous" },
    { id: "MP026", name: "ban-drop-table", note: "DROP TABLE is destructive" },
    { id: "MP034", name: "ban-drop-database", note: "database-level destruction" },
  ],
  MP037: [
    { id: "MP041", name: "ban-char-field", note: "CHAR(n) is even worse than VARCHAR(n)" },
    { id: "MP007", name: "no-column-type-change", note: "changing VARCHAR length requires rewrites" },
    { id: "MP040", name: "prefer-timestamptz", note: "use correct types from the start" },
  ],
  MP038: [
    { id: "MP039", name: "prefer-identity-over-serial", note: "use IDENTITY instead of SERIAL" },
    { id: "MP045", name: "require-primary-key", note: "every table needs a primary key" },
    { id: "MP007", name: "no-column-type-change", note: "INT to BIGINT migration is very expensive" },
  ],
  MP039: [
    { id: "MP015", name: "no-add-column-serial", note: "ADD COLUMN with SERIAL has similar issues" },
    { id: "MP038", name: "prefer-bigint-over-int", note: "use BIGINT for key columns" },
    { id: "MP045", name: "require-primary-key", note: "every table needs a primary key" },
  ],
  MP040: [
    { id: "MP037", name: "prefer-text-over-varchar", note: "choose correct types from the start" },
    { id: "MP041", name: "ban-char-field", note: "avoid problematic types" },
    { id: "MP007", name: "no-column-type-change", note: "changing types later is very expensive" },
  ],
  MP041: [
    { id: "MP037", name: "prefer-text-over-varchar", note: "VARCHAR is better than CHAR but TEXT is best" },
    { id: "MP040", name: "prefer-timestamptz", note: "use correct types from the start" },
    { id: "MP007", name: "no-column-type-change", note: "changing types later is expensive" },
  ],
  MP042: [
    { id: "MP001", name: "require-concurrent-index-creation", note: "indexes should use CONCURRENTLY" },
    { id: "MP023", name: "require-if-not-exists", note: "indexes should use IF NOT EXISTS" },
    { id: "MP027", name: "disallowed-unique-constraint", note: "UNIQUE constraints need named indexes" },
  ],
  MP043: [
    { id: "MP030", name: "require-not-valid-check", note: "CHECK constraints should use NOT VALID" },
    { id: "MP037", name: "prefer-text-over-varchar", note: "use TEXT with CHECK for validation" },
    { id: "MP007", name: "no-column-type-change", note: "domain changes affect all dependent columns" },
  ],
  MP044: [
    { id: "MP007", name: "no-column-type-change", note: "all type changes require table rewrites" },
    { id: "MP038", name: "prefer-bigint-over-int", note: "start with BIGINT to avoid future narrowing" },
    { id: "MP030", name: "require-not-valid-check", note: "use CHECK constraints for validation" },
  ],
  MP045: [
    { id: "MP038", name: "prefer-bigint-over-int", note: "use BIGINT for primary keys" },
    { id: "MP039", name: "prefer-identity-over-serial", note: "use IDENTITY for auto-increment PKs" },
    { id: "MP023", name: "require-if-not-exists", note: "CREATE TABLE should use IF NOT EXISTS" },
  ],
  MP046: [
    { id: "MP001", name: "require-concurrent-index-creation", note: "CONCURRENTLY for index creation" },
    { id: "MP008", name: "no-multi-ddl-transaction", note: "DDL should not be wrapped in transactions" },
    { id: "MP004", name: "require-lock-timeout", note: "set lock_timeout before DDL" },
  ],
  MP047: [
    { id: "MP006", name: "no-vacuum-full", note: "VACUUM FULL also rewrites the entire table" },
    { id: "MP032", name: "ban-cluster", note: "CLUSTER also rewrites the entire table" },
    { id: "MP020", name: "require-statement-timeout", note: "long-running operations need timeouts" },
  ],
  MP048: [
    { id: "MP003", name: "volatile-default-table-rewrite", note: "ADD COLUMN with volatile default" },
    { id: "MP011", name: "unbatched-data-backfill", note: "backfills should be batched" },
    { id: "MP015", name: "no-add-column-serial", note: "SERIAL creates implicit volatile default" },
  ],
  MP049: [
    { id: "MP045", name: "require-primary-key", note: "every table should have a primary key" },
    { id: "MP038", name: "prefer-bigint-over-int", note: "use BIGINT for primary keys" },
    { id: "MP023", name: "require-if-not-exists", note: "CREATE TABLE should use IF NOT EXISTS" },
  ],
  MP050: [
    { id: "MP001", name: "require-concurrent-index", note: "CREATE INDEX must use CONCURRENTLY" },
    { id: "MP042", name: "require-index-name", note: "indexes should have explicit names" },
  ],
  MP051: [
    { id: "MP001", name: "require-concurrent-index", note: "CREATE INDEX must use CONCURRENTLY" },
    { id: "MP042", name: "require-index-name", note: "indexes should have explicit names" },
    { id: "MP050", name: "prefer-hnsw-over-ivfflat", note: "use HNSW for vector indexes" },
  ],
  MP052: [
    { id: "MP017", name: "no-drop-column", note: "DROP COLUMN acquires ACCESS EXCLUSIVE lock" },
    { id: "MP010", name: "no-rename-column", note: "renaming columns breaks application queries" },
    { id: "MP007", name: "no-column-type-change", note: "changing column types requires table rewrite" },
  ],
  MP053: [
    { id: "MP008", name: "no-multi-ddl-transaction", note: "avoid multiple DDL in one transaction" },
    { id: "MP025", name: "ban-concurrent-in-transaction", note: "CONCURRENTLY cannot run inside a transaction" },
    { id: "MP054", name: "alter-type-add-value-in-transaction", note: "new enum values not visible until COMMIT" },
  ],
  MP054: [
    { id: "MP012", name: "no-enum-add-in-transaction", note: "ALTER TYPE ADD VALUE cannot run in transaction (PG < 12)" },
    { id: "MP024", name: "no-enum-value-removal", note: "DROP TYPE destroys enum and dependent columns" },
    { id: "MP053", name: "ban-uncommitted-transaction", note: "BEGIN without matching COMMIT" },
  ],
  MP055: [
    { id: "MP045", name: "require-primary-key", note: "tables should always have a PK" },
    { id: "MP017", name: "no-drop-column", note: "dropping columns requires ACCESS EXCLUSIVE lock" },
  ],
  MP056: [
    { id: "MP042", name: "require-index-name", note: "indexes should have explicit names" },
    { id: "MP001", name: "require-concurrent-index", note: "CREATE INDEX must use CONCURRENTLY" },
  ],
  MP057: [
    { id: "MP026", name: "ban-drop-table", note: "dropping tables is destructive" },
    { id: "MP022", name: "no-drop-cascade", note: "CASCADE silently drops dependent objects" },
  ],
  MP058: [
    { id: "MP004", name: "require-lock-timeout", note: "SET lock_timeout before DDL" },
    { id: "MP008", name: "no-multi-ddl-transaction", note: "avoid multiple DDL in one transaction" },
  ],
  MP059: [
    { id: "MP015", name: "no-add-column-serial", note: "SERIAL creates implicit sequence" },
    { id: "MP039", name: "prefer-identity-over-serial", note: "GENERATED AS IDENTITY is safer" },
  ],
  MP060: [
    { id: "MP012", name: "no-enum-add-in-transaction", note: "ALTER TYPE ADD VALUE cannot run in transaction" },
    { id: "MP024", name: "no-enum-value-removal", note: "dropping enum values is destructive" },
    { id: "MP054", name: "alter-type-add-value-in-transaction", note: "new enum value not visible until COMMIT" },
  ],
  MP070: [
    { id: "MP097", name: "ban-drop-constraint-backing-index", note: "the drop this rule stands down for" },
    { id: "MP021", name: "require-concurrent-reindex", note: "REINDEX without CONCURRENTLY blocks writes" },
    { id: "MP009", name: "require-drop-index-concurrently", note: "DROP INDEX without CONCURRENTLY" },
  ],
  MP084: [
    { id: "MP002", name: "require-check-not-null-pattern", note: "the same CHECK pattern for an existing column" },
    { id: "MP018", name: "no-force-set-not-null", note: "SET NOT NULL scans the whole table" },
    { id: "MP003", name: "volatile-default-table-rewrite", note: "when the DEFAULT you add is volatile" },
  ],
  MP085: [
    { id: "MP091", name: "warn-privilege-drift", note: "grants mixed into schema migrations at all" },
    { id: "MP073", name: "ban-superuser-role", note: "superuser operations in migrations" },
  ],
  MP086: [
    { id: "MP005", name: "require-not-valid-foreign-key", note: "add FKs with NOT VALID to avoid a full scan" },
    { id: "MP016", name: "require-index-on-fk", note: "an unindexed FK makes cascades and parent deletes slow" },
    { id: "MP069", name: "warn-fk-lock-both-tables", note: "adding an FK locks both sides" },
    { id: "MP074", name: "require-deferrable-fk", note: "DEFERRABLE FKs for bulk operations" },
  ],
  MP087: [
    { id: "MP003", name: "volatile-default-table-rewrite", note: "volatile DEFAULT on a new column" },
    { id: "MP048", name: "ban-alter-default-volatile-existing", note: "volatile DEFAULT on an existing column" },
    { id: "MP030", name: "require-not-valid-check", note: "add CHECK constraints with NOT VALID first" },
  ],
  MP088: [
    { id: "MP011", name: "unbatched-data-backfill", note: "UPDATE with no WHERE rewrites everything at once" },
    { id: "MP067", name: "warn-backfill-no-batching", note: "unbounded DELETE" },
    { id: "MP080", name: "ban-data-in-migration", note: "keeping DML out of DDL migrations entirely" },
  ],
  MP089: [
    { id: "MP007", name: "no-column-type-change", note: "the rewrite any ALTER COLUMN TYPE causes" },
    { id: "MP083", name: "warn-fk-nondeterministic-collation", note: "non-deterministic collations on FK columns" },
    { id: "MP001", name: "require-concurrent-index-creation", note: "building the replacement index online" },
  ],
  MP090: [
    { id: "MP064", name: "ban-disable-trigger", note: "disabling triggers breaks replication and audit" },
    { id: "MP065", name: "ban-lock-table", note: "explicit locks in migrations" },
  ],
  MP091: [
    { id: "MP085", name: "warn-grant-widening", note: "grants that are too broad regardless of which file they live in" },
    { id: "MP080", name: "ban-data-in-migration", note: "the same separation, applied to DML" },
    { id: "MP073", name: "ban-superuser-role", note: "superuser operations in migrations" },
  ],
  MP092: [
    { id: "MP001", name: "require-concurrent-index-creation", note: "the general rule this one qualifies" },
    { id: "MP046", name: "require-concurrent-detach-partition", note: "DETACH PARTITION CONCURRENTLY" },
    { id: "MP094", name: "require-attach-partition-check", note: "avoiding the ATTACH validation scan" },
    { id: "MP070", name: "warn-concurrent-index-invalid", note: "cleaning up after a failed concurrent build" },
  ],
  MP093: [
    { id: "MP072", name: "warn-partition-default-scan", note: "the ATTACH-time scan of the default partition" },
    { id: "MP094", name: "require-attach-partition-check", note: "avoiding the other ATTACH scan" },
    { id: "MP049", name: "require-partition-key-in-pk", note: "partition key must be in the primary key" },
  ],
  MP094: [
    { id: "MP072", name: "warn-partition-default-scan", note: "the other scan ATTACH can trigger" },
    { id: "MP093", name: "warn-default-partition-growth", note: "what makes that other scan expensive" },
    { id: "MP046", name: "require-concurrent-detach-partition", note: "the reverse operation" },
    { id: "MP030", name: "require-not-valid-check", note: "the NOT VALID then VALIDATE pattern generally" },
  ],
  MP095: [
    { id: "MP006", name: "no-vacuum-full", note: "another full-relation rewrite under ACCESS EXCLUSIVE" },
    { id: "MP032", name: "ban-cluster", note: "CLUSTER rewrites the table the same way" },
    { id: "MP047", name: "ban-set-logged-unlogged", note: "SET LOGGED/UNLOGGED also rewrites" },
    { id: "MP004", name: "require-lock-timeout", note: "bounding the wait on any blocking DDL" },
  ],
  MP096: [
    { id: "MP033", name: "require-concurrent-refresh-matview", note: "refreshing without blocking readers" },
    { id: "MP080", name: "ban-data-in-migration", note: "long-running data work inside DDL migrations" },
  ],
  MP097: [
    { id: "MP055", name: "drop-pk-replica-identity-break", note: "dropping a primary key breaks logical replication" },
    { id: "MP027", name: "disallowed-unique-constraint", note: "adding a unique constraint safely" },
    { id: "MP022", name: "no-drop-cascade", note: "CASCADE dropping dependents silently" },
    { id: "MP009", name: "require-drop-index-concurrently", note: "dropping ordinary indexes without blocking" },
    { id: "MP070", name: "warn-concurrent-index-invalid", note: "the drop-first retry convention, and where it does not apply" },
  ],
  MP098: [
    { id: "MP028", name: "no-rename-table", note: "renaming has the same breaking effect" },
    { id: "MP010", name: "no-rename-column", note: "the column-level equivalent" },
    { id: "MP052", name: "warn-dependent-objects", note: "views and functions that may break" },
    { id: "MP071", name: "ban-rename-in-use-column", note: "the add-copy-drop alternative to renaming" },
  ],
  MP099: [
    { id: "MP073", name: "ban-superuser-role", note: "superuser privileges in migrations" },
    { id: "MP085", name: "warn-grant-widening", note: "overly broad grants, including to PUBLIC" },
    { id: "MP057", name: "rls-enabled-without-policy", note: "another silent access-control failure" },
  ],
  MP100: [
    { id: "MP016", name: "require-fk-index", note: "the opposite problem, a missing index" },
    { id: "MP101", name: "warn-index-on-write-hot-table", note: "what an extra index costs on the write path" },
    { id: "MP042", name: "require-index-name", note: "indexes should have explicit names" },
  ],
  MP101: [
    { id: "MP001", name: "require-concurrent-index", note: "CREATE INDEX must use CONCURRENTLY" },
    { id: "MP013", name: "high-traffic-table-ddl", note: "query volume rather than write volume" },
    { id: "MP100", name: "warn-redundant-index", note: "an index that costs writes and buys nothing" },
    { id: "MP104", name: "warn-long-index-build", note: "how long the build itself will take" },
  ],
  MP102: [
    { id: "MP006", name: "no-vacuum-full", note: "VACUUM FULL takes ACCESS EXCLUSIVE for the whole rewrite" },
    { id: "MP007", name: "no-column-type-change", note: "type changes that rewrite the table" },
    { id: "MP014", name: "large-table-ddl", note: "the lock duration side of the same operations" },
    { id: "MP103", name: "warn-replication-lag-risk", note: "the WAL these rewrites send to replicas" },
  ],
  MP103: [
    { id: "MP011", name: "unbatched-data-backfill", note: "a WHERE-less UPDATE, flagged without a database" },
    { id: "MP067", name: "warn-backfill-no-batching", note: "the same for DELETE" },
    { id: "MP102", name: "warn-rewrite-disk-headroom", note: "the disk side of a rewrite" },
    { id: "MP055", name: "drop-pk-replica-identity", note: "replication correctness rather than lag" },
  ],
  MP104: [
    { id: "MP001", name: "require-concurrent-index", note: "CREATE INDEX must use CONCURRENTLY" },
    { id: "MP070", name: "warn-concurrent-index-invalid", note: "what a failed CONCURRENTLY build leaves behind" },
    { id: "MP101", name: "warn-index-on-write-hot-table", note: "what the index costs after the build" },
    { id: "MP014", name: "large-table-ddl", note: "lock duration on large tables generally" },
  ],
  MP105: [
    { id: "MP111", name: "warn-timescale-columnstore-ddl", note: "ALTER forms TimescaleDB refuses outright" },
    { id: "MP106", name: "prefer-timescale-drop-chunks", note: "deleting old data from a hypertable" },
    { id: "MP110", name: "warn-partitioned-parent-fanout", note: "the same fan-out for native partitioning" },
    { id: "MP001", name: "require-concurrent-index", note: "which does not apply to hypertables" },
  ],
  MP106: [
    { id: "MP105", name: "warn-timescale-hypertable-ddl", note: "DDL against a hypertable" },
    { id: "MP067", name: "warn-backfill-no-batching", note: "a WHERE-less DELETE, flagged without a database" },
    { id: "MP103", name: "warn-replication-lag-risk", note: "what a per-row DELETE sends to replicas" },
  ],
  MP107: [
    { id: "MP058", name: "multi-alter-table-same-table", note: "combine ALTERs to take the lock once" },
    { id: "MP004", name: "require-lock-timeout", note: "always set lock_timeout before DDL" },
    { id: "MP110", name: "warn-partitioned-parent-fanout", note: "the same fan-out shape, single-node" },
  ],
  MP108: [
    { id: "MP072", name: "warn-partition-default-scan", note: "ATTACH PARTITION scans the DEFAULT partition" },
    { id: "MP046", name: "require-concurrent-detach-partition", note: "DETACH without CONCURRENTLY" },
    { id: "MP110", name: "warn-partitioned-parent-fanout", note: "DDL cost on a parent with many partitions" },
  ],
  MP109: [
    { id: "MP050", name: "prefer-hnsw-over-ivfflat", note: "which access method to use in the first place" },
    { id: "MP112", name: "warn-hnsw-build-memory", note: "what an HNSW build needs to run fast" },
    { id: "MP104", name: "warn-long-index-build", note: "build duration on large tables" },
  ],
  MP110: [
    { id: "MP049", name: "require-partition-key-in-pk", note: "partition key requirements" },
    { id: "MP072", name: "warn-partition-default-scan", note: "ATTACH PARTITION scans the DEFAULT partition" },
    { id: "MP105", name: "warn-timescale-hypertable-ddl", note: "the same fan-out for hypertables" },
    { id: "MP107", name: "warn-citus-distributed-ddl", note: "the same fan-out across worker nodes" },
  ],
  MP111: [
    { id: "MP105", name: "warn-timescale-hypertable-ddl", note: "DDL fan-out across chunks" },
    { id: "MP007", name: "no-column-type-change", note: "why type changes are expensive anywhere" },
    { id: "MP057", name: "rls-without-policy", note: "row-level security without a policy" },
  ],
  MP112: [
    { id: "MP109", name: "require-vector-index-params", note: "setting m and ef_construction deliberately" },
    { id: "MP050", name: "prefer-hnsw-over-ivfflat", note: "which access method to use" },
    { id: "MP104", name: "warn-long-index-build", note: "build duration for ordinary indexes" },
  ],
};
