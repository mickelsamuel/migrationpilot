{
  "name": "migrationpilot",
  "version": "0.1.0",
  "description": "Know exactly what your PostgreSQL migration will do to production — before you merge.",
  "type": "module",
  "bin": {
    "migrationpilot": "./dist/cli.cjs"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "action.yml"
  ],
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "esbuild src/cli.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/cli.cjs --external:pg-native --external:libpg-query && esbuild src/action/index.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/action/index.js --external:pg-native --external:libpg-query && node -e \"const fs=require('fs'),p=require('path');const src=fs.realpathSync('node_modules/libpg-query');const dst='dist/action/node_modules/libpg-query';fs.mkdirSync(p.dirname(dst),{recursive:true});fs.cpSync(src,dst,{recursive:true})\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "pnpm run build"
  },
  "keywords": [
    "postgresql",
    "postgres",
    "migration",
    "database",
    "schema",
    "safety",
    "linter",
    "ci",
    "github-action",
    "devops"
  ],
  "author": "Micke Samuel <mickelsamuel.b@gmail.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/mickelsamuel/migrationpilot"
  },
  "homepage": "https://migrationpilot.dev",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.0.0",
    "@pgsql/types": "^17.6.2",
    "chalk": "^5.6.2",
    "cli-table3": "^0.6.5",
    "commander": "^14.0.3",
    "libpg-query": "^17.7.3",
    "pg": "^8.18.0",
    "pgsql-parser": "^17.9.11"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.3.15",
    "@eslint/js": "^10.0.1",
    "@types/node": "^25.2.3",
    "@types/pg": "^8.16.0",
    "esbuild": "^0.27.3",
    "eslint": "^10.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.55.0",
    "vitest": "^4.0.18"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "@electric-sql/pglite"]
  }
}
